# 更新日志（CHANGES.md）

本文件是发布说明的**唯一事实源**：每个版本发布前在此**顶部**追加一段
`## vX.Y.Z（YYYY-MM-DD）`，`scripts/make-release-manifest.mjs` 会把对应段落
提取为 latest.json 的 `notes`，应用内「设置→关于→检查更新」会以 markdown
渲染展示给用户。段落内容支持 GFM（列表、表格、代码块、链接等）。

**条目规矩**：
- 每一条写成 `类型（修复/功能名）：这一条的说明文字`——开头是类型，取「修复」
  或具体功能名，后跟中文冒号与说明文字。例如 `修复：搜索偶发重复结果。`、
  `快捷搜索：新增置顶分组快捷筛选。`。

## v0.1.2（2026-09-04）

- 文字识别（OCR）：新增识图打标签——导入图片后自动在后台识别图中的文字并追加为标签（文件名标签保留，搜索时两路都能命中），用文字就能搜到表情；提供三种引擎：系统 OCR（本地离线，默认开启）、Tesseract（本地开源引擎，自动检测安装并附下载引导）、AI Studio PaddleOCR（云端，对表情包风格化文字更准，按张消耗每日免费额度）；已识别过的表情可强制重新识别，存量表情支持一键批量补跑。
- 文字识别（OCR）：AI Studio 云端引擎支持内嵌窗口一键登录百度账号——Access Token 与每日额度自动获取，无需再手动申请；设置页新增额度条实时显示今日用量（接近上限转红警示），未登录、额度用尽、请求过频均有明确中文提示；识别模型可选 PP-OCRv6 / PP-OCRv5（默认 PP-OCRv6）。
- 修复：AI Studio 云端识别报「HTTP 500 Internal Server Error」——百度已将 PaddleOCR 在线接口整体切换为异步任务协议，旧同步接口已下线；现改为对接官方异步 API（提交任务 → 轮询状态 → 拉取结果），已填写的旧版 API 地址会自动迁移到官方端点，无需重新配置。
- 标签：标签选择弹窗重设计为双栏结构——左栏「当前标签」、右栏「标签库」按需搜索添加，支持行内重命名、勾选多个标签批量从表情移除、以及全局删除标签；所有改动即时生效，识别过的图片也可在弹窗内直接重跑 OCR。
- 图库：新增「刷新图库」按钮；导入的新图现在按当前排序正确落位（此前会堆在列表末尾，要切一次排序才能看到），导入与刷新的落地改为平滑过渡，不再整屏闪烁。
- 更新：发现新版本改为弹窗提示，展示更新说明并支持在弹窗内直接下载与安装，不再需要前往设置页；弹窗重新设计——品牌色头部直接展示新版本号、当前版本与安装包大小，更新说明改为独立滚动卡片；新增「今日不再提醒」，关闭后当天不再自动重弹。
- 更新：更新弹窗的默认下载源自动选中检查更新时成功命中的镜像（检查走官方直连时默认选「官方直连」项），选中的源直接显示在下拉框上，不再需要展开才能看到；下载中关闭弹窗会自动取消下载，同时只允许一个下载任务，避免重复下载。
- 修复：应用启动时偶发/必现的「无法读取本地表情库」错误（数据库未就绪的时序问题），现在启动即可正常加载素材库。
- 修复：设置弹窗结构被误改导致界面常驻无法关闭的问题，现在关闭按钮 / Esc / 点击遮罩均可正常关闭。
- 修复：「最近使用」视图的搜索框不生效、始终显示全部记录的问题。
- 修复：浏览大图库与保存标签后的网格意外滚动 / 跳动（焦点恢复与列表锚定等多处来源）；标签弹窗内容超高时底部按钮不再被顶出弹窗。
- 界面：镜像源测速结果统一为三档延迟显示（良好 / 一般 / 较慢），测速失败原因直接显示在对应行上；镜像源摘要行准确区分可用、失败与未测速数量，卡片布局与标题行图标对齐。

## v0.1.1（2026-09-02）

- 修复：快捷键冲突的错误信息在设置弹窗内以单行溢出窗口的问题，现在会自动换行显示。
- 界面：主窗口顶部工具栏移除 "EmoBox" 字样。
- 发布：Release 附件不再附带 `.sha256` 校验文件，校验和改为直接展示在 Release 说明末尾的表格中。

## v0.1.0（2026-09-02）

- 首次发布：EmoBox 首个公开版本。
- 素材库：图片导入（文件 / 文件夹 / 拖拽 / 剪贴板收藏）与 SHA-256 + 感知双重去重；分组、标签、收藏、回收站、最近使用。
- 快捷搜索：全局快捷键唤起浮层，支持 `组*标签` 精确语法、选中文字自动搜索、复制后自动粘贴回原窗口。
- GIF 动画：全链路保真，悬停播放、复制粘贴到微信/QQ 保留动画。
- 自动检查更新：内置 GitHub 加速镜像源（可自定义、可测速排序），安装包经 SHA-256 校验后启动安装程序。
- 系统托盘：托盘图标与自绘 Fluent 菜单。
- 主题：浅色 / 深色 / 跟随系统。
