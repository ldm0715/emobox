# 表情匣主窗口手工验收清单

> 界面视觉和交互由用户手工审核；自动检查只覆盖构建、类型检查和 Rust 测试。

## 唯一导入入口

- [ ] 正常有内容时，顶部工具栏只有一个“导入”MenuButton；
- [ ] 内容区标题栏没有第二个“导入表情”按钮；
- [ ] 侧栏没有“导入文件夹”按钮；
- [ ] 空状态只有一个“导入表情”MenuButton；
- [ ] 顶部和空状态复用同一个 ImportMenu；
- [ ] “导入文件夹”真实可用；
- [ ] “导入图片”未实现时 disabled；
- [ ] “从剪贴板收藏”未实现时 disabled。

## 顶部工具栏

- [ ] 左侧依次显示侧栏折叠按钮、应用图标和“表情匣”；
- [ ] 侧栏收起时隐藏应用名称；
- [ ] 中间搜索框 placeholder 为“搜索表情、标签或文件名”；
- [ ] 右侧只保留“导入”和主题快速菜单；
- [ ] 顶部没有设置齿轮；
- [ ] 主题菜单包含跟随系统、浅色、深色；
- [ ] 当前主题有明确选中状态；
- [ ] `Ctrl+F` 聚焦搜索框；
- [ ] 搜索框有内容时 Esc 清空，无内容时 Esc 失焦。

## 侧栏展开状态

- [ ] 宽度约 220～240px；
- [ ] 显示“资料库”标题；
- [ ] 导航为全部表情、最近使用、收藏；
- [ ] 显示“我的分组”和新增分组按钮；
- [ ] 无分组时显示“还没有分组”；
- [ ] 底部显示 Keyboard 图标；
- [ ] 显示 `Ctrl + Alt + Space`；
- [ ] 显示“在聊天时快速找图”；
- [ ] 设置入口位于侧栏最底端。

## 侧栏收起状态

- [ ] 宽度约 56px；
- [ ] 只显示导航图标；
- [ ] 导航图标均有 Tooltip；
- [ ] 不显示“资料库”“我的分组”等文字；
- [ ] 分组只显示图标并通过 Tooltip 显示名称；
- [ ] 快捷键说明缩减为 Keyboard 图标；
- [ ] Keyboard Tooltip 为 `Ctrl + Alt + Space：在聊天时快速找图`；
- [ ] 设置只显示图标并有 Tooltip；
- [ ] 中央表情网格自动获得更多宽度；
- [ ] 重启后保持上次折叠状态。

## 设置 Dialog

- [ ] 点击左下角设置入口打开设置 Dialog；
- [ ] 设置包含常规、快捷键、存储与导入、关于；

### 常规

- [ ] 主题可选择跟随系统、浅色、深色；
- [ ] 主题与顶部主题菜单使用同一状态；
- [ ] “关闭窗口时最小化到系统托盘”显示为固定启用，并说明需通过托盘“退出”结束进程；
- [ ] 默认启动页面可选择全部表情、最近使用、收藏；
- [ ] 重启后使用保存的默认页面。

### 快捷键

- [ ] 默认显示 `Ctrl + Alt + Space` 快速搜索；
- [ ] 可以直接编辑或录制新的组合键并应用；
- [ ] 修改成功后侧栏和浮层标题同步显示新快捷键；
- [ ] 重启后保持已保存的快捷键；
- [ ] 输入 `Alt + Space` 时显示 Windows 系统窗口菜单冲突警告；
- [ ] 输入无修饰键的组合时显示明确的格式错误；
- [ ] 注册已被其他应用占用的快捷键时显示错误 Toast 和设置页错误说明；
- [ ] 注册失败时不覆盖上一个仍可用的快捷键；
- [ ] 显示 `Ctrl + Alt + S` 从剪贴板收藏，但明确说明本阶段未实现；
- [ ] “打开浮层”按钮打开独立快捷搜索窗口。

### 存储与导入

- [ ] 显示当前表情素材目录；
- [ ] 未选择目录时显示“尚未导入文件夹”；
- [ ] 在资源管理器中打开尚未实现时 disabled；
- [ ] 显示递归扫描本地目录的索引方式；
- [ ] 显示 PNG、JPG、JPEG、GIF、WEBP。

### 关于

- [ ] 显示表情匣 / EmoBox；
- [ ] 显示版本 0.1.0；
- [ ] 说明图片保留在用户原始目录；
- [ ] 已实现和未实现能力如实展示。

## 导入和表情网格

- [ ] 导入文件夹后递归扫描仍正常；
- [ ] 扫描期间显示 ProgressBar；
- [ ] 完成后显示导入数量 Toast；
- [ ] 损坏图片不会中断扫描；
- [ ] 首页不展示技术指标；
- [ ] 表情网格支持三档密度；
- [ ] 排序支持名称 A–Z、名称 Z–A、文件格式；
- [ ] 搜索实时过滤文件名；
- [ ] 收藏在当前会话中生效；
- [ ] 未实现菜单项处于 disabled。

## 快捷搜索浮层

- [ ] 点击侧栏 Keyboard 按钮可以打开独立浮层；
- [ ] 设置 > 快捷键 > 打开浮层可以打开同一个独立窗口；
- [ ] 在其他应用中按 `Ctrl + Alt + Space` 可以唤出浮层；
- [ ] 浮层显示时再次按全局快捷键可以隐藏；
- [ ] 每次唤出时搜索框自动聚焦且上次查询已清空；
- [ ] 输入文件名片段时结果实时过滤，大小写不敏感；
- [ ] 鼠标移入结果时选中状态跟随，单击后复制图片、显示成功状态并隐藏；
- [ ] 左右方向键逐项移动，上下方向键按网格行移动；
- [ ] Enter 复制当前选中图片，成功后更新最近使用并隐藏；
- [ ] Esc 隐藏窗口，之后仍可再次唤出；
- [ ] 浮层不会出现在任务栏中，并保持在其他窗口上方；
- [ ] 复制失败时显示错误，浮层保持打开；
- [ ] 应用不自动切换窗口、自动粘贴或自动发送。

## Windows 图片剪贴板

- [ ] PNG：复制后粘贴到画图，显示实际图片而不是路径；
- [ ] JPG/JPEG：复制后粘贴到画图，尺寸和内容正确；
- [ ] WebP：复制后粘贴到画图或其他图片软件，显示转换后的静态图片；
- [ ] GIF：复制后只粘贴首帧，成功 Toast 明确说明动画不会保留；
- [ ] 记事本中粘贴不会出现文件路径、`file://` URL 或纯文本；
- [ ] Word、微信、QQ、飞书或另一款支持图片粘贴的软件至少验证一种；
- [ ] 成功复制后最近使用按最新在前显示，重复复制不会产生重复项；
- [ ] 源文件不存在、无法读取或解码失败时显示错误，浮层不关闭。

## 范围确认

- [ ] 已实现系统托盘、关闭隐藏和托盘真正退出；
- [ ] 未实现自动粘贴或自动发送；
- [ ] 未实现单文件导入；
- [ ] 已实现最近使用 JSON 持久化，不使用云端、账号或 SQLite；
- [ ] 未实现收藏持久化；
- [ ] 未实现分组 CRUD；
- [ ] 未实现标签搜索；
- [ ] 未实现删除；

## 第四阶段：系统托盘、关闭隐藏和最近使用持久化

### 准备

1. 在 PowerShell 进入项目目录：

   ```powershell
   cd "C:\Users\宫城楠木\Desktop\EmoBox"
   npm run tauri dev
   ```

2. 准备至少 3 张名称容易区分的图片，例如 `alpha.png`、`beta.jpg`、`gamma.webp`；
3. 打开任务管理器“详细信息”页，准备观察 `emobox.exe`；
4. 如托盘图标被 Windows 折叠，先展开任务栏通知区域。

### A. 关闭按钮只隐藏主窗口

1. 启动应用并确认主窗口可见；
2. 点击主窗口标题栏右上角关闭按钮；
3. 确认主窗口消失；
4. 在任务管理器确认 `emobox.exe` 仍存在；
5. 确认系统托盘仍显示表情匣图标；
6. 按已注册的全局快捷键（默认 `Ctrl + Alt + Space`），确认主窗口隐藏后搜索浮层仍能打开；
7. 按 Esc 隐藏搜索浮层。

预期：关闭按钮只隐藏主窗口，不退出进程，不注销全局快捷键。

### B. 托盘三个菜单项

1. 右键或单击托盘图标打开菜单；
2. 确认菜单只有并至少包含以下三个可用项：
   - `打开主窗口`；
   - `打开搜索浮层`；
   - `退出`；
3. 选择“打开主窗口”，确认窗口恢复、显示并获得焦点；
4. 再次关闭主窗口，然后选择“打开搜索浮层”，确认独立浮层显示且搜索框获得焦点；
5. 暂不点击“退出”，继续下面的最近使用验收。

### C. 记录路径、时间和使用次数

1. 在主窗口导入测试图片所在文件夹；
2. 打开搜索浮层，复制 `alpha.png` 一次；
3. 再次打开浮层，复制 `beta.jpg` 一次；
4. 再次打开浮层，复制 `alpha.png` 第二次；
5. 打开以下文件：

   ```powershell
   notepad "$env:APPDATA\com.emobox.app\recent-images.json"
   ```

6. 确认 JSON 中每条记录都包含：
   - `item.path`：原始图片绝对路径；
   - `lastUsedAt`：毫秒时间戳；
   - `useCount`：累计使用次数；
7. 确认 `alpha.png` 只有一条记录、位于最前、`useCount` 为 `2`；
8. 确认 `beta.jpg` 的 `useCount` 为 `1`；
9. 确认应用没有在数据目录复制任何原始图片。

预期：重复使用去重并移动到最前，次数递增，时间更新，最多保留 50 条。

### D. 空搜索优先最近使用

1. 保持已导入目录中至少还有 `gamma.webp` 等未使用图片；
2. 打开搜索浮层，不输入任何文字；
3. 确认前两个结果依次为最近最后使用的 `alpha.png`、`beta.jpg`；
4. 确认未使用的当前索引图片排在最近使用之后；
5. 输入 `gamma`，确认仍能搜索当前索引中的非最近图片；
6. 清空搜索，确认再次恢复最近使用优先顺序。

### E. 重启后仍保留最近使用

1. 通过托盘菜单选择“退出”；
2. 在任务管理器确认 `emobox.exe` 已结束；
3. 再次执行 `npm run tauri dev` 启动应用；
4. 不重新导入目录，进入主窗口“最近使用”，确认 `alpha.png` 和 `beta.jpg` 仍存在且顺序不变；
5. 打开搜索浮层并保持空搜索，确认最近使用仍优先展示；
6. 选择 `alpha.png` 复制，确认原文件仍存在时可以成功写入剪贴板；
7. 再次查看 `recent-images.json`，确认 `alpha.png` 的 `useCount` 从 `2` 增加到 `3`，`lastUsedAt` 更新。

预期：最近使用记录跨进程重启保留；持久化记录可独立于当前内存扫描索引用于展示和再次复制。

### F. 托盘退出真正结束应用

1. 关闭主窗口使其隐藏；
2. 从托盘菜单选择“退出”；
3. 确认主窗口和搜索浮层都消失；
4. 确认托盘图标消失；
5. 在任务管理器确认 `emobox.exe` 不再存在；
6. 再按全局快捷键，确认不会再打开搜索浮层。

预期：托盘“退出”是真正退出，不是再次隐藏。

### G. 范围和异常检查

- [ ] 未新增账号、登录、同步或网络上传；
- [ ] 未新增 SQLite 或其他数据库；
- [ ] 设置继续使用 `localStorage: emobox.settings`；
- [ ] 最近使用只新增本地 JSON 文件 `recent-images.json`；
- [ ] 原始图片没有被复制、移动、重命名或删除；
- [ ] 仅对专用测试副本临时改名或移出测试目录后，再次复制会显示可读错误且应用不崩溃；测试后恢复该副本；
- [ ] JSON 不存在时应用以空记录正常启动；
- [ ] JSON 内容损坏时应用记录警告并以空记录继续启动。
