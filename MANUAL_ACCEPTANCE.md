# 表情匣主窗口手工验收清单

> 界面视觉和交互由用户手工审核；自动检查只覆盖构建、类型检查和 Rust 测试。

## 唯一导入入口

- [ ] 正常有内容时，顶部工具栏只有一个“导入”MenuButton；
- [ ] 内容区标题栏没有第二个“导入表情”按钮；
- [ ] 侧栏没有“导入文件夹”按钮；
- [ ] 空状态只有一个“导入表情”MenuButton；
- [ ] 顶部和空状态复用同一个 ImportMenu；
- [ ] “导入文件夹”真实可用；
- [ ] “导入图片”未实现时 disabled；
- [ ] “从剪贴板收藏”未实现时 disabled。

## 顶部工具栏

- [ ] 左侧依次显示侧栏折叠按钮、应用图标和“表情匣”；
- [ ] 侧栏收起时隐藏应用名称；
- [ ] 中间搜索框 placeholder 为“搜索表情、标签或文件名”；
- [ ] 右侧只保留“导入”和主题快速菜单；
- [ ] 顶部没有设置齿轮；
- [ ] 主题菜单包含跟随系统、浅色、深色；
- [ ] 当前主题有明确选中状态；
- [ ] `Ctrl+F` 聚焦搜索框；
- [ ] 搜索框有内容时 Esc 清空，无内容时 Esc 失焦。

## 侧栏展开状态

- [ ] 宽度约 220～240px；
- [ ] 显示“资料库”标题；
- [ ] 导航为全部表情、最近使用、收藏；
- [ ] 显示“我的分组”和新增分组按钮；
- [ ] 无分组时显示“还没有分组”；
- [ ] 底部显示 Keyboard 图标；
- [ ] 显示 `Ctrl + Alt + Space`；
- [ ] 显示“在聊天时快速找图”；
- [ ] 设置入口位于侧栏最底端。

## 侧栏收起状态

- [ ] 宽度约 56px；
- [ ] 只显示导航图标；
- [ ] 导航图标均有 Tooltip；
- [ ] 不显示“资料库”“我的分组”等文字；
- [ ] 分组只显示图标并通过 Tooltip 显示名称；
- [ ] 快捷键说明缩减为 Keyboard 图标；
- [ ] Keyboard Tooltip 为 `Ctrl + Alt + Space：在聊天时快速找图`；
- [ ] 设置只显示图标并有 Tooltip；
- [ ] 中央表情网格自动获得更多宽度；
- [ ] 重启后保持上次折叠状态。

## 设置 Dialog

- [ ] 点击左下角设置入口打开设置 Dialog；
- [ ] 设置包含常规、快捷键、存储与导入、关于；

### 常规

- [ ] 主题可选择跟随系统、浅色、深色；
- [ ] 主题与顶部主题菜单使用同一状态；
- [ ] 关闭窗口时最小化到托盘在未实现时 disabled；
- [ ] 默认启动页面可选择全部表情、最近使用、收藏；
- [ ] 重启后使用保存的默认页面。

### 快捷键

- [ ] 默认显示 `Ctrl + Alt + Space` 快速搜索；
- [ ] 可以直接编辑或录制新的组合键并应用；
- [ ] 修改成功后侧栏和浮层标题同步显示新快捷键；
- [ ] 重启后保持已保存的快捷键；
- [ ] 输入 `Alt + Space` 时显示 Windows 系统窗口菜单冲突警告；
- [ ] 输入无修饰键的组合时显示明确的格式错误；
- [ ] 注册已被其他应用占用的快捷键时显示错误 Toast 和设置页错误说明；
- [ ] 注册失败时不覆盖上一个仍可用的快捷键；
- [ ] 显示 `Ctrl + Alt + S` 从剪贴板收藏，但明确说明本阶段未实现；
- [ ] “打开浮层”按钮打开独立快捷搜索窗口。

### 存储与导入

- [ ] 显示当前表情素材目录；
- [ ] 未选择目录时显示“尚未导入文件夹”；
- [ ] 在资源管理器中打开尚未实现时 disabled；
- [ ] 显示递归扫描本地目录的索引方式；
- [ ] 显示 PNG、JPG、JPEG、GIF、WEBP。

### 关于

- [ ] 显示表情匣 / EmoBox；
- [ ] 显示版本 0.1.0；
- [ ] 说明图片保留在用户原始目录；
- [ ] 已实现和未实现能力如实展示。

## 导入和表情网格

- [ ] 导入文件夹后递归扫描仍正常；
- [ ] 扫描期间显示 ProgressBar；
- [ ] 完成后显示导入数量 Toast；
- [ ] 损坏图片不会中断扫描；
- [ ] 首页不展示技术指标；
- [ ] 表情网格支持三档密度；
- [ ] 排序支持名称 A–Z、名称 Z–A、文件格式；
- [ ] 搜索实时过滤文件名；
- [ ] 收藏在当前会话中生效；
- [ ] 未实现菜单项处于 disabled。

## 快捷搜索浮层

- [ ] 点击侧栏 Keyboard 按钮可以打开独立浮层；
- [ ] 设置 > 快捷键 > 打开浮层可以打开同一个独立窗口；
- [ ] 在其他应用中按 `Ctrl + Alt + Space` 可以唤出浮层；
- [ ] 浮层显示时再次按全局快捷键可以隐藏；
- [ ] 每次唤出时搜索框自动聚焦且上次查询已清空；
- [ ] 输入文件名片段时结果实时过滤，大小写不敏感；
- [ ] 鼠标移入结果时选中状态跟随，单击确认并隐藏；
- [ ] 左右方向键逐项移动，上下方向键按网格行移动；
- [ ] Enter 确认当前选中项并隐藏；
- [ ] Esc 隐藏窗口，之后仍可再次唤出；
- [ ] 浮层不会出现在任务栏中，并保持在其他窗口上方；
- [ ] 当前不执行或伪造剪贴板复制、自动粘贴和自动发送。

## 范围确认

- [ ] 未实现系统托盘；
- [ ] 未实现图片剪贴板；
- [ ] 未实现自动粘贴或自动发送；
- [ ] 未实现单文件导入；
- [ ] 未实现最近使用持久化；
- [ ] 未实现收藏持久化；
- [ ] 未实现分组 CRUD；
- [ ] 未实现标签搜索；
- [ ] 未实现删除；
