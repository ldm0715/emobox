# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **硬性规则：本文件与 `AGENTS.md` 内容保持一致——每次更新本文件后，整体复制覆盖 `AGENTS.md`（`cp CLAUDE.md AGENTS.md`）。**

## 这是什么

EmoBox —— 一款 Windows 优先的 Tauri v2 桌面应用（GPL-3.0 开源，https://github.com/ldm0715/emobox），用于管理本地表情图片素材，并快速搜索/复制到剪贴板。有三个顶层窗口：主窗口 `main`、瞬态 `quick-search` 浮层和 `tray-menu` 托盘菜单窗口（Phase 26，替代原生 Win32 托盘菜单）。用户原始图片保持原位；EmoBox 只把"导入"的图片复制到自己的 app-data 目录做受管存储（Phase 8 起不再有"仅索引原路径"模式）。

## 构建 / 检查 / 测试命令

> 环境要求（来自 README）：Windows 10/11；Node.js 22+；Rust stable（target `x86_64-pc-windows-msvc`）+ MSVC C++ Build Tools；Microsoft Edge WebView2 Runtime。

```powershell
npm install
npm run tauri dev          # 启动应用（Vite + Tauri 一起）
npm run build              # tsc --noEmit + vite build
npm run tauri build -- --no-bundle   # 产出 src-tauri/target/release/emobox.exe

# Rust 工具链（所有路径相对仓库根目录，用 `cmd` 或 pwsh 执行）
cargo fmt --check --manifest-path src-tauri/Cargo.toml
cargo check --manifest-path src-tauri/Cargo.toml
cargo test  --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings

# 单测（按名称子串过滤，如 sanitize_trims / same_session_reuse_via_peek）
cargo test --manifest-path src-tauri/Cargo.toml <测试名子串>

# JS 测试：全部 / 单文件 / 按用例名过滤
npx vitest run
npx vitest run src/features/library/useMultiSelection.test.ts
npx vitest run -t "<用例名>"
```

验收标准是 `cargo check` + `cargo clippy -- -D warnings` + `cargo test` + `npm run build`（tsc + vite）+ `npx vitest run`（Phase 8 起浮层 `useQuickSearchQuery` 乱序/分页/缓存（Phase 20）测试、Phase 9 起 `useMultiSelection` 多选测试、Phase 27 起 `mirrorSources` 镜像源测试）+ `MANUAL_ACCEPTANCE.md` 中的手动清单。

## 高层架构

### 前端（`src/`，React 19 + TS + Fluent UI v9）

- `src/main.tsx` 读取 `getCurrentWindow().label`，挂载 `<App />`（主窗口）、`<QuickSearchWindow />`（浮层）或 `<TrayMenuWindow />`（Phase 26 托盘菜单窗口）。三个窗口是各自独立的 React 树，不是路由；透明窗口（quick-search / tray-menu）按 label 给 `<html>` 挂类名（`global.css` 据此置透明背景）。
- `src/App.tsx` 持有大部分 UI 状态。**Phase 17 分页**：`currentEmojis: IndexedEmoji[]` 只持有当前视图**已加载页**（`PAGE_SIZE = 200`，滚动到底经 `loadMore` 追加），总数在 `viewTotal` / `hasMore`，`nextOffsetRef` 是下一页 offset 游标（按服务端返回行数前进，**不要**用 `currentEmojis.length` 做 offset——本地删除/去重会让两者错位，全被去重的页会死循环）；`viewSeqRef` 作废在途的迟到页响应。按视图 useEffect（deps `[currentView, debouncedQuery, sortOption, recentItems, groups, tags]`）拉第 1 页：非回收站/非最近视图走 `fetchViewPage` → `searchEmojis({view, query, sort, limit: 200, offset: 0})`，trash 走 `listDeletedEmojis({limit, offset})`，recent 走本地 `recentItems` 派生（上限 50、客户端过滤排序、不分页）；**排序已下推到 SQL**（`filteredItems` 不再客户端排序，recent 除外）。计数与已加载条数解耦：`allCount` / `favoriteCount` / `trashCount` 全部来自后端 total（`refreshLibrary` 用 `limit: 0` 纯计数请求）；`favoriteIds: Set<number>` 随每页加载合并（`mergeFavoriteFlags`）+ `toggleFavorite` 乐观更新；`indexedById`（标签交集初选）从 `currentEmojis` 派生。**Phase 9 多选**：`selectedIds: Set<number>` 由 `useMultiSelection(filteredItems)` 托管（网格唯一选中源），另有 `multiSelectMode: boolean`（显式多选开关，头部「多选」按钮触发，开启后单击切换选中；切视图经 `prevViewRef` effect 统一清选区 + 关模式）。**Phase 17 全选**：多选模式下头部「全选/取消全选」按钮（`allSelected = selectedIds.size >= filteredItems.length`），语义 = **只选已加载项**，直接复用 hook 的 `selectAll()` / `clear()`。**Phase 18**：新增 `previewItem: IndexedImage | null`（双击卡片的大图预览项，App 层持有——`keyShortcutRef` 弹窗豁免与复制/收藏句柄都在这层）与 `previewMeta` memo（经 `indexedById` + `groups` / `tagById` 解析预览项的分组/标签名）；卡片点击 Tag 走 `handleTagClick` → `setSearchQuery(`*${tag}`)` 复用 `*标签` 精确语法。**Phase 18 性能不变量（承重）**：传给 `EmojiGridItem` 的所有 props 身份必须稳定——`handleCopy` / `handleTagClick` 是 useCallback，`handleItemSelect`（rangeSelect 依赖 anchor）与 `toggleFavorite`（依赖 favoriteIds）经 latest-ref 读依赖，`viewItems` 投影与 `tagsByPath` 的 names 数组按 emoji 引用走 WeakMap identity 缓存（tagById 变化时 tagsByPath 缓存整体失效防读旧名）——否则 `React.memo(EmojiGridItem)` 失效、整网格每次 App 渲染全量重渲染（Phase 18 真机踩过）。**Phase 22**：`currentViewRef` 已上移到 `currentView` 声明处；`getCurrentGroupId()`（稳定 useCallback，读 ref）在四个导入入口（导入图片/导入文件夹/拖放/剪贴板收藏）**发起时**捕获目标分组传给后端，`prepareAfterImport(targetGroupId)` 在「仍停留在该分组视图」时不切回 all，改为经 `viewReloadTick` state（视图 effect deps 之一）重拉第 1 页——landingKey 不变所以不重播入场动画；toast 经 `groupNameForToast`（按发起时 id 查 `groups`）注明「已加入分组「X」」。
- `src/components/ThemeProvider.tsx` 是*设置*上下文（不只是主题）。把 `theme`、`sidebarCollapsed`、`sidebarGroupsCollapsed`（「我的分组」区折叠）、`defaultView`、`quickSearchShortcut`、`clipboardCollectShortcut`、`autoPaste`、`selectionSearch`、`downloadWebGif`、`closeToTray`（**Phase 25，可选三态**：`undefined`=未选择→点 ✕ 弹询问窗 / `true`=已记住驻留托盘 / `false`=已记住直接退出；**关闭询问弹窗勾「记住」与设置开关拨动写的是同一项**）、`autoCheckUpdates` / `updateMirrors`（**Phase 27**：启动静默检查更新开关[默认开]与 GitHub 加速镜像源列表[默认 gh-proxy.com / ghproxy.net / ghfast.top 三源，可增删、测速后按延迟升序持久化，官方直连由 Rust 侧恒定兜底]）持久化到 `localStorage: emobox.settings`，并通过 `getCurrentWindow().setTheme(...)` 把 `theme` 同步给原生窗口。默认快捷键来自 `src/config/shortcuts.ts`。**Phase 15**：`selectionSearch` 在挂载和变更时经 `setSelectionSearchEnabled` 命令推送到 Rust 的 `SelectionSearchState`（内存镜像，localStorage 仍是事实源；两个窗口都推、幂等）。**Phase 25**：`closeToTray` 同模式经 `set_close_to_tray(Option<bool>)` 推送到 `CloseBehaviorState`（`undefined` 推 `null`）。**Phase 16**：`downloadWebGif`（联网下载网页 GIF，默认关）不做 Rust 内存镜像——`App.tsx` 每次收藏时把它作为 `downloadWebGif` 参数传给 `collect_image_from_clipboard` 命令。**Phase 19**：`resolvedTheme` 变化时设置 `document.documentElement.style.colorScheme`——滚动条等 WebView 原生控件跟随主题的关键（`FluentProvider` 只切组件 CSS 变量、`setTheme` 只管原生窗口，都不管滚动条）；同一 effect 还会同步 `<meta name="theme-color">`。**主题 token override（2026-09 Surface 层级重构）**：`darkTheme` / `lightTheme` 在 spread `createDarkTheme/LightTheme(brand)` 后逐项覆盖颜色 token——暗色为深灰蓝阶梯（BG2 `#191d26` 最暗 → BG1 `#222732` 内容区 → BG3 `#2a303d` 卡片最亮，修正了 Fluent 默认暗色"卡片比背景黑"的反向层级；选中态 `colorSubtleBackgroundSelected` 低饱和深蓝 + 品牌指示条；`colorBrandBackground2` 统一选中卡/拖放提示/tint 徽章的品牌面）；亮色只微调 `colorNeutralStroke2`（分割线+卡片边框）与 `colorSubtleBackgroundSelected`（浅品牌蓝）。全应用 17 个样式文件只用 token、零散落硬编码色——**改主题观感一律去 ThemeProvider override token，不要在组件里写十六进制**（仅存例外：EmojiGridItem 悬停按钮组压图 scrim `rgba(24,24,27,…)` 与 global.css `::selection` 走 CSS 变量 `var(--colorBrandBackground)`）。
- `src/lib/tauri.ts` 是唯一调用 `invoke()` 的地方。新增 Tauri 命令就在这里加类型化包装；不要从特性代码里直接调 `invoke`。Phase 6 加了约 20 个分组/标签/收藏/搜索/回收站包装。
- `src/types.ts` 是共享契约的唯一来源。Phase 8 起 `IndexedImage`（7 字段，含 `id`，网格缩略图按 id 取）与 `IndexedEmoji`（15 字段，含 `id`、`sourceType`、`isFavorite`、`groupIds[]`、`tagIds[]`、`lastUsedAt`、`usageCount`、`importedAt`、`modifiedAt`）并存，`sourceType` 联合为 `"managed_import" | "clipboard"`（外部扫描已移除）。**Phase 17**：`search_emojis` / `list_deleted_emojis` 返回分页结构 `SearchResult { items: IndexedEmoji[]; total: number }`；`SearchOptions.sort` 是 7 值联合 `SearchSort`（`recent` / `name-asc` / `name-desc` / `format` / `added-time` / `modified-time`，与 Rust ORDER BY 分支一一对应）。字段名用 camelCase，因为 Rust 侧用 `#[serde(rename_all = "camelCase")]`。
- 特性文件夹：`features/import`（唯一的 `ImportMenu`，工具栏与空状态共用；**Phase 23 起 `useLibraryImport` hook** 托管四条导入入口的动作集与加载条状态——`isImporting` 经 `beginImport`/`endImport` 管控，新导入入口必须走这对辅助，不要自己 set state）、`features/library`（网格视图 + Phase 6 对话框 `GroupDialog` / `MoveToGroupDialog` / `TagPickerDialog` + 共享确认弹窗 `ConfirmDialog`（UI 一致性整改起替代原生 `window.confirm/prompt`）+ Phase 18 大图预览 `EmojiPreviewDialog` + `useDebouncedValue` + Phase 9 多选 `useMultiSelection` hook + Phase 16 悬停播放 `useGifPreview` hook + **Phase 18 单双击消歧 `useClickIntent` hook** + `isGifExtension` + 共享样式 `cardStyles.ts`（卡片透明描边 reset / 选中态品牌描边环，主网格与浮层结果卡共用）与 `pickerDialogStyles.ts`（TagPicker/MoveToGroup 弹窗共用的列表行样式））、`features/search`（浮层窗口内容）。Hook 紧挨着拥有它们的组件。**Phase 17**：`EmojiGrid` 的 IntersectionObserver 哨兵是双通道——`visibleCount < items.length` 时渐进渲染下一批 72 个 DOM，渲染完已加载项且 `hasMore` 时改调 `onLoadMore()` 拉数据页；`visibleCount` 只在 `resetKey` 变化时重置（2026-09 起 resetKey = App 的 `viewGeneration` 落地代数字符串，即新视图数据落地时才重置——见「主窗口分页」条目的 keep-previous 机制），追加页不清零（否则滚动跳变）。**Phase 18 卡片结构**：`EmojiGridItem`（已 `React.memo`）是 root（flex column）→ `frame`（`position:relative`、1:1，**全部悬浮元素收在 frame 内**——GIF 徽标 / 多选框 / 悬停按钮组[星标·复制·更多，trash 视图只留「更多」——回收站不允许复制/收藏，只走菜单或批量条的 恢复/彻底删除]，root 加了 caption 行后锚在 root 的绝对定位会错位）→ caption 行（文件名 ellipsis + **原生 `title`** 展示全名，选中态品牌色加粗；紧凑密度右端内联 `Tag16Regular`+数量）→ tagRow（仅 comfortable/large 密度、有标签时：≤2 个 Fluent `Badge outline` + `+N`，单个 maxWidth 84px 截断[Badge 无 truncate prop，需 className 覆盖 `display:"block"` 才能文本截断]，点击 `onTagClick` 筛选、点击/双击都 stopPropagation）。**交互**：普通单击 = 250ms 延迟复制（`useClickIntent`，双击第二击 `event.detail > 1` 取消；**不选中**；**trash 视图例外：单击退化为 replace 选中，复制在回收站整体不可用**）、双击 = 大图预览（trash 视图预览为 `readOnly`，隐藏 收藏/复制 按钮）、Ctrl/Shift/多选模式点击 = 立即选中（不进延迟）、Enter/Space = replace 选中、右键 = 共享菜单照旧。**Phase 19 拖入提示**：`EmojiLibraryView` 新增 `dragActive` prop（App 的 Tauri 拖放事件传入）+ `contentWrap`（`position: relative` 包裹层）；dropOverlay 是 contentWrap 内 `position: absolute; inset: 16px 36px 16px 16px`（右边多收 20px 避开滚动条）的品牌色放置提示——**只盖图片网格区**，不盖 LibraryHeader / 状态条；样式 `colorBrandBackground2` + 1px 实线 `colorBrandStroke1` + `borderRadiusXLarge`（不用虚线框）。tokens 是字符串，inset 算术要直接写字面量。**trash 视图不进拖放导入（2026-09 越权收紧）**：App 的 drag `enter` 仅在 `currentViewRef.current !== "trash"` 时置 `dragActive`（回收站不显示放置提示），`handleDroppedPaths` 读 ref 拒绝 drop 并弹 info toast「回收站视图不支持导入图片」（`handleImportImages` / `handleImportFolder` 同款守卫；全局快捷键 `Ctrl+Alt+S` 与视图无关、保持可用）。
- `src/app/LibrarySidebar.tsx` — 侧边栏。Phase 13 起布局从上到下固定：全部表情/最近使用/收藏 → 我的分组（标题栏左侧 chevron 折叠开关 + 右侧 🔍/＋，**折叠状态 `sidebarGroupsCollapsed` 经 ThemeProvider 持久化**，折叠时 `groupList` 容器仍挂载、只清空内容——它是唯一 `flex:1` 弹性区，卸载会让底部固定项重排；搜索/新建按钮仅展开时显示、`searchOpen` 随折叠强制关闭；搜索收进按钮、点击才展开 `SearchBox`，`searchOpen` 参与过滤条件 → 收起即失效）→ 分组列表（**唯一 `flex:1; min-height:0; overflow-y:auto` 的弹性滚动区**，占满剩余高度）→ 未分组/回收站 → 快捷键（单行）/设置（**Phase 19 起两行都是 `hintButton` 自定义按钮**——「设置」原为 Fluent `Button`，与快捷键行盒模型不一致导致内容一个偏上一个偏下；统一成同一组件，勿改回）。固定项紧凑（导航行 28px、`navigation` 行距 8px、分隔线 4px、根 padding 6px），不要再给固定项 `flex` 或大留白。**底部 `bottom` 容器不要加 `gap`**：夹在快捷键/设置两按钮之间的内层 `Divider` 自带 8px 上下 margin，容器 gap 会与之叠加翻倍（内侧 16px vs 外侧 8px/6px），导致快捷键按钮在两条分隔线之间偏上、设置按钮偏下（2026-09 修复）——两按钮间距只靠 Divider 自身 margin 提供。**分组行**：左侧图标位由 `getGroupIcon(group.icon)`（`src/features/library/groupIcons.ts` 注册表，SQLite `groups.icon` 存注册名、NULL/未知名回退 `Folder24Regular`）渲染，中间名称（ellipsis + 常驻 Tooltip 展示全名）、右侧数量 Badge；置顶图钉保留。分组「⋯」菜单含 置顶/更改图标（`GroupIconPickerDialog`，状态在 App 的 `iconPickerGroup`）/重命名/删除（重命名走 App 持有的 `GroupDialog` rename 模式（`renameGroupState`），删除走组件内 `ConfirmDialog`——UI 一致性整改后**不要用原生 `window.confirm/prompt`**，原生框不跟随应用主题）。**新建分组弹窗**（`GroupDialog` create 模式）自带图标行：默认文件夹 + `POPULAR_GROUP_ICONS`（17 个，**增删保持总数 18**，9 列网格恰好两整行），`onSubmit(name, icon)` → App 先 `createGroup` 再 `setGroupIcon`；`MoveToGroupDialog` 的内联新建不带图标（默认文件夹，建后走菜单改）。**视觉**：`navItemSelected` 选中态 = 浅蓝背景 + `& > svg` 品牌色图标 + 左侧 3px 品牌指示条（基础/选中样式抽在 `src/app/navItemStyles.ts`，与设置对话框左导航共享，改一处两处生效）；`navItem`/`hintButton` 悬停用 `colorSubtleBackgroundHover`。**Fluent `SearchBox` 坑**：根元素是 inline-flex、只有 `max-width: 468px` 没有 `width: 100%`——在普通块容器里会收缩成内容宽度并贴左（侧栏分组搜索框曾因此偏左），要居中/撑满需外层 `display:flex; justify-content:center` 容器或显式给宽度；**即使给了 `width:"100%"`，仍会被 `max-width: 468px` 截成 468px 贴左，要真正撑满必须同时 `maxWidth: "none"`**（浮层搜索框 Phase 20 踩过）。**Phase 14**：`divider` 样式显式 `flexGrow: 0` + `flexShrink: 0`——Fluent `Divider` 默认 `flex-grow:1`，曾让 3 个分隔线与分组列表平分剩余高度、分组区只剩 3 行（详见 Phase 14 文档与「关键不变量」）。置顶走 `group.isPinned`（后端 `list_groups` ORDER BY 排前）+ 右键菜单「置顶/取消置顶」+ 名字前小图钉；折叠态搜索重置、`overflow-x:hidden` 防滚动条压窄图标行。详见 `docs/phase13-sidebar-groups-redesign.md` 与 `docs/phase14-sidebar-divider-flex-fix.md`。
- `src/app/AppToolbar.tsx` — 顶部工具栏（品牌区 / 搜索框 / 导入与批量操作；「导入」按钮经 `showImport` 按视图显隐——App 传 `showImport={currentView !== "trash"}`，回收站视图不渲染）。左端「展开/收起侧栏」按钮：24px 图标（与侧栏导航图标同规格）+ 4px 左边距（`sidebarToggle`，视觉上与侧栏内容留呼吸位，不要精确对齐到侧栏图标列的 20px——实测太靠右）。

### 后端（`src-tauri/src/`，Rust + Tauri 2）

- `lib.rs` — 入口。装配插件（`clipboard-manager`、`dialog`、`global-shortcut`、`log`）、托管状态（`ShortcutRegistry`、`DatabaseState`、`RecentImagesState`、`TargetWindowState`、`SelectionSearchState`、`CloseBehaviorState`、`updater::UpdateState`）、托盘设置、`on_window_event` 的 `CloseRequested`：`quick-search` 与 `tray-menu`（Phase 26）永远隐藏，`main` 按 `CloseBehaviorState`（Phase 25：`Some(true)`→hide / `Some(false)`→`app.exit(0)` / `None`→`emit_to(main,"main-close-requested")` 让前端弹询问窗、窗口保持可见，见 `docs/phase25-close-behavior-dialog.md`），`setup` 里调用 `ShortcutRegistry::reconcile` 在启动时 `unregister_all` 清掉残留的 OS 级快捷键，并对两个透明圆角窗口（`quick-search` / `tray-menu`）统一跑 `apply_rounded_overlay_style`（`set_shadow(false)` + `dwm::disable_nc_rendering` + `dwm::apply_rounded_region` 三件套，见「Surface 层级」）。托盘「退出」菜单项与已记住的「直接退出」都调用 `app.exit(0)`。`invoke_handler!` 注册 46 个命令（Phase 8 删 `scan_directory` / `get_indexed_images`，加 `import_folder`；Phase 13 加 `set_group_pinned`；Phase 15 加 `set_selection_search_enabled`；分组图标改造加 `set_group_icon`；Phase 24 加 `open_external_url`——https 前缀 + host 白名单（目前仅 github.com）校验后经 explorer.exe/`open` 打开系统浏览器，全应用唯一打开外部链接的通道，新增外链调用方要同步扩白名单；Phase 25 加 `set_close_to_tray` / `exit_application`；Phase 26 加 `tray_menu_action`；Phase 27 加 `check_for_update` / `start_update_download` / `cancel_update_download` / `install_pending_update` / `test_mirror_speed`）。
- `commands.rs` — 所有 `#[tauri::command]` 处理器，经 `tauri::generate_handler![...]` 在 `lib.rs` 注册。长时间工作用 `tauri::async_runtime::spawn_blocking` 包裹；其余同步。Phase 8 起：所有库数据变更命令（导入 / 回收站四件套 / 收藏 / 分组 / 标签 / 剪贴板收藏）成功后 `quick_search::notify_library_changed(&app)`；导入命令带 `skip_perceptual_dedup: Option<bool>`，**Phase 22 起带 `target_group_id: Option<i64>`**（分组视图内发起导入时的目标分组，命令层对 `id <= 0` 过滤、service 层 `ensure_target_group_exists` 预检）；`load_thumbnail` 改按 `emoji_id` 查 DB。Phase 6 新增：`list/create/rename/delete_group`、`list/create/rename/delete_tag`、`add/remove_emojis_to/from_group`、`add/remove_tags_to/from_emojis`、`set_emojis_favorite`、`search_emojis`、`soft_delete_to_trash`、`restore_from_trash`、`permanently_delete_emojis`、`empty_trash`、`list_deleted_emojis`、`show_in_explorer`。**Phase 13 新增** `set_group_pinned`（置顶分组，同步命令、不调 `notify_library_changed`——置顶不改表情数据，不影响快速搜索结果）；**分组图标改造新增** `set_group_icon(id, icon: Option<String>)`（同 `set_group_pinned` 模式：纯侧栏外观变更，同步命令、不刷成员 `updated_at`、不发 notify；`GroupDto` 含 `icon`）。回收站命令返回 `TrashResult { succeeded, files_moved, failures }`；**Phase 17**：`search_emojis` 与 `list_deleted_emojis`（新增可选 `limit`/`offset` 参数）均返回 `SearchPage { items: Vec<IndexedEmoji>, total: i64 }`（`groupIds` / `tagIds` 已由 `fill_relations` 填好；`limit: 0` = 纯计数用法）。
- `clipboard.rs` — 写路径：PNG/JPEG/WebP 经 `image::load_from_memory_with_format` 编码，GIF 用 `GifDecoder`（仅首帧），通过 `tauri-plugin-clipboard-manager` 写 RGBA。返回描述来源/剪贴板格式和动画状态的 `ClipboardCopyOutcome`。这里不发射任何事件；命令层发 `image-copied`。**Phase 6 加了 SQLite 回写**：复制成功后 `copy_image_to_clipboard` 也调用 `EmojiRepository::record_image_used(id, at_ms)` 保持 `last_used_at` / `usage_count` 同步（SQLite 是"最近使用"的新单一事实源）。**Phase 16**：GIF 在插件写完 RGBA 首帧后，`append_gif_animation` 经 `platform/windows/clipboard_raw.rs` **追加** CF_HDROP 文件列表（主通道，指向受管 `.gif` —— 实测微信/QQ 只按文件粘贴才保动画，不消费 `image/gif` 位图格式）+ 注册格式 `"image/gif"` 的原始字节（辅通道），均不 `EmptyClipboard`、不破坏插件写的 DIB/PNG，成功 → `animationPreserved=Some(true)`，失败 → warn + 维持 `Some(false)`，绝不影响复制主流程。
- `clipboard_collect.rs` — 读路径：**Phase 16 起最前按序尝试三条动画保真通道** —— ①注册格式 `"image/gif"` 原始字节（Firefox 复制 GIF 时放）；②`CF_HDROP` 文件路径（QQ 复制聊天图片 / 资源管理器复制 .gif 文件时放，指向 `nt_qq\nt_data\Pic\...\Ori\*.gif` 原图，**只读**源文件字节、用源文件名）；③网页 GIF URL 联网下载（`attempt_web_gif`，**设置开关 `downloadWebGif` 默认关**：Chrome/Edge 复制只放首帧位图 + URL；开启时 ureq 下载 —— 仅 http(s) 且 `.gif` 结尾、连接 5s/读取 15s 超时、20 MB 上限；关闭时静态导入 + message 提醒）。三通道都无 → 静默降级原 RGBA 路径：`app.clipboard().read_image()` → RGBA → `RgbaImage::from_raw` → `DynamicImage` → `AssetService::stage_dynamic_image` → `ImportService::import_dynamic_image`。**2026-09：`read_image`（arboard）失败后 Windows 上走原生回退链 `read_image_native_fallback`**（"PNG" 注册格式字节 → `CF_DIBV5` → `CF_DIB`，自家 image 流水线解码；补 arboard 两大缺口——只解 "PNG" 且失败不回退、从不试 CF_DIB[QQ 静态图只放老 DIB]）。回退链里 Chrome 形态的 DIB（32bpp + BI_RGB + alphaMask=0xff000000）走 `decode_dib_32bpp_bgra` **手解 BGRA** 保 alpha：image 0.25.10 对 V3/V4/V5 头 + BI_BITFIELDS 会跳过头后 12 字节（假定头后还有一份 RGB 掩码），arboard 的 `maybe_tweak_header` 改 BI_BITFIELDS 方案在当前 image 版本下解不开它自家的 Chrome 样本（实测复现）——**勿改回 tweak 方案**（测试 `decode_clipboard_dib_decodes_arboard_chrome_sample` 锁定）。其余 DIB 走 `BmpDecoder::new_without_file_header`（Cargo.toml 的 image features 需含 `bmp`）。**2026-09 补充（通道 0.1 `try_collect_file_drop_image`）**：资源管理器复制**非 GIF** 受支持图片文件时剪贴板只有 CF_HDROP 路径、无任何位图格式（arboard 读不到，此前误报「剪贴板中没有图片」）→ 取第一个受支持文件（`scanner::supported_extension` 白名单去 GIF，GIF 仍由动画通道优先）**只读**源文件字节，`image::load_from_memory` 防扩展名伪造后 `import_bytes` 按原始字节导入（用源文件名）；结果映射经 `import_bytes_to_outcome` 与 GIF 通道共用。详见 `docs/phase21-clipboard-dib-fallback.md`。返回 `ClipboardCollectOutcome` 枚举（Empty / Imported / Duplicate / Failed / Unavailable）而非 `Result`；D2 错误分类在这里（见不变量）。`clipboard_filename(extension)` 按参数生成 `.png` / `.gif` 文件名。**这是剪贴板链路唯一的网络行为**（下载用户刚复制的 URL，不上传）；全应用另一处联网是 Phase 27 的检查更新/下载安装包（`updater.rs`，见下）。
- `scanner.rs` — 目录遍历器（`collect_image_files`，walkdir 递归、只收受支持扩展名）**并定义 `IndexedImage` / `IndexedEmoji`**。Phase 8 删除了 `scan_directory` / `ScanSummary` / `scan_and_persist`（外部索引模式整体移除）。
- `thumbnail.rs` — 缩略图 data URL。**磁盘缓存优先**：`load_thumbnail(emoji_id, max_size)` 按 id 查 DB 的 `thumbnail_path`，缓存存在且非空 → 直接 base64（不重编码）；缺失 → 回退解码原图生成。`write_thumbnail_png` 在导入时写磁盘缓存（Fast 压缩，不再 `sync_all`）。
- `recent.rs` — 内存 `RecentImagesState` 缓存，现在**镜像** SQLite 的 `EmojiRepository::search_recent`（50 条）。Phase 6 让 SQLite 成为事实源：`copy_image_to_clipboard` 经 `record_image_used` 写 SQLite；`get_recent_images` 经 `search_recent` + `fill_relations_for_recent` 读。`recent-images.json` 文件不再写入；首次启动时导入一次（`database::import_legacy_recent_if_present`），之后只读以向后兼容。
- `tray.rs` — 系统托盘（**Phase 26 重写**，见「托盘菜单窗口」）：不再构建原生 Win32 菜单——`TrayIconBuilder` 只有 tooltip + 图标；`on_tray_icon_event` 左键 Up → `show_main_window`（复用既有 unminimize/show/set_focus 三步）、右键 Up → `show_tray_menu`（按事件 `rect` 定位弹出 `tray-menu` 窗口）；`handle_menu_action` 消费前端传来的 `TrayMenuAction`（kebab-case 枚举，`tray_menu_action` 命令转调）。
- `updater.rs` — **Phase 27 自动更新（GitHub Releases + 镜像前缀加速）**。检查 = 拉 Release 资产 `latest.json`（`version/pubDate/notes/platforms{windows-x86_64:{url,sha256,size}}`，camelCase）→ `semver` 比较（容忍 `v` 前缀）；**源顺序 = 用户镜像列表 → 官方直连兜底**（`candidate_urls` 恒追加直连并去重，用户误填 github.com 本尊会被 `mirror_is_github_direct` 跳过）；下载（`download_and_stage`）流式写临时文件、**边下边算 SHA-256** 与清单比对、每 ≥256KB 发 `update-download-progress` 事件（emit_to main）、上限 200MB、`UpdateState`（`Arc<Inner>` 手工 Clone 共享，Mutex pending + AtomicBool cancel）支持取消，任何失败/取消都删临时文件；`install_pending_update` spawn NSIS 安装器 + `app.exit(0)`（失败把 pending 放回可重试）；`test_mirror_speed` 经镜像拉 raw README（1MB 截断）计整次耗时——选 raw README 是因为不依赖是否已发布 Release。**发布链路**：CHANGES.md（每版本一段 `## vX.Y.Z（日期）`）→ `scripts/make-release-manifest.mjs`（SHA-256 + 提取段落为 notes）→ GitHub Release 上传 setup.exe + latest.json；应用内更新说明经 react-markdown 渲染（**默认不渲染原始 HTML**，天然防镜像注入）。镜像规范化规则前后端同源：`mirrorSources.ts::normalizeMirror`（URL 解析 + http(s) + 有 host + 补尾斜杠）/ Rust `join_mirror`。详见 `docs/phase27-updates-and-branding.md`。
- **应用图标/品牌资源（Phase 27）**：事实源是 `static/logo.ico` → System.Drawing 提取 `static/logo.png` → `npx tauri icon static/logo.png` 重新生成 `src-tauri/icons/*`；前端显示用 `src/assets/logo.png`（设置弹窗标题栏品牌行）。**窗口/任务栏图标是编译期嵌入的**（tauri_winres 的 exe 资源 + `generate_context!` 的 default_window_icon 两处），且 tauri-build 2.6 的 rerun-if-changed **不监听 icons/**——已在 `build.rs` 显式声明 `cargo:rerun-if-changed=icons/icon.ico`，换图标后重启 dev 即触发重嵌入+重编译；重启后若资源管理器/任务栏偶尔仍显旧图，是 Windows shell 图标缓存（非应用问题）。
- `quick_search.rs` — 薄壳：`WINDOW_LABEL`、`show_quick_search` / `hide_quick_search`（均泛型 `<R: Runtime>`，托盘 / 主窗口 / 全局快捷键三条路径**共用** —— Phase 15 前快捷键路径内联复制 show 逻辑且从不捕获粘贴目标，是既有 bug，已修）、`normalize_shortcut` / `shortcut_parser_text` 辅助函数，以及 Phase 8 的 `LIBRARY_CHANGED_EVENT` / `notify_library_changed`（数据变更命令调它给浮层发 `library-changed`）。浮层内容由 `QuickSearchWindow` + `useQuickSearchQuery` 驱动（见"快捷搜索浮层"）。**Phase 7**：`show_quick_search` 在 `center/show/set_focus` *之前*调用 `target_window::capture_from_foreground`（这样浮层打开前的前台窗口会在 EmoBox 抢焦点前被捕获；**Phase 20**：`center()` 仅首次显示执行——窗口只隐藏不销毁，拖拽后的位置跨唤起保留，重启重置）；`hide_quick_search` 只隐藏、**不清空目标**（前端自动粘贴链是 hide-then-paste 顺序，清空会让每次粘贴 `noTarget` 降级 —— Phase 15 修复；跨会话复用由 capture 的"打开即先 clear"防住）。**Phase 15**：其后紧接着调 `selection_capture::capture_selected_text`（选中文字也必须在抢焦点前读 —— UIA `GetFocusedElement` 在浮层 show 之后会指向浮层自己），`quick-search-opened` 的 payload 从 `()` 改为 `QuickSearchOpenedPayload { selectedText: Option<String> }`（camelCase）。
- `platform/` — Phase 7 的 Windows 专属 FFI，也是 `windows` crate + `unsafe` 唯一出现的地方。`platform/mod.rs` 把整棵树藏在 `#[cfg(windows)]` 后面；非 Windows 构建不含它。`windows/foreground_window.rs`（经 `GetForegroundWindow` + `GetWindowThreadProcessId` + `GetWindowTextW` + `OpenProcess`/`QueryFullProcessImageNameW` 捕获前台 HWND / PID / 标题 / 进程名）、`windows/window_activation.rs`（`validate` 用捕获时的 PID 复验防 HWND 复用；`activate` 恢复最小化窗口并用 Drop guard 包住 `AttachThreadInput` 保证 detach 总会执行；轮询 `GetForegroundWindow` 最多 500ms）、`windows/input_simulation.rs`（`SendInput` 发 Ctrl+V / Ctrl+C（Phase 15），均拆成 4 个离散事件、间隔 20ms，用 `VK_LCONTROL`；`click_at` 模拟鼠标）、`windows/focus_restore.rs`（UIA 定位输入控件并点击它；**Phase 15 修复了 E0133 编译损坏** —— UIA 条件查询用 `CreateTrueCondition()` + `FindAll(TreeScope_Subtree)` + 遍历 `CurrentControlType() == UIA_EditControlTypeId`，不用 `CreatePropertyCondition`/VARIANT；EnumChildWindows 回退保留）、`windows/selection_reader.rs`（Phase 15：UIA TextPattern 读前台焦点控件的当前选区，非侵入通道，失败返回 `None` 走 Ctrl+C 兜底）、`windows/clipboard_raw.rs`（Phase 16：Win32 原生剪贴板读写 —— 读注册格式 `"image/gif"` 原始字节（Firefox）与 `CF_HDROP` 文件路径（QQ/资源管理器，`read_file_drop` + `parse_drop_files` 仅支持宽字符列表）；写 CF_HDROP 文件列表（`write_file_drop`，微信/QQ 粘贴动图的已验证通道）+ 注册格式字节。`ClipboardGuard` Drop guard 保证 CloseClipboard；读路径任何失败 → `None` 静默降级；写路径 `SetClipboardData` 成功后 HGLOBAL 所有权转移给系统**不得 free**、失败路径才 `GlobalFree`。**坑**：windows 0.61 里 `GlobalFree` 在 `Win32::Foundation` 而非 `System::Memory`；`GetClipboardData`/`SetClipboardData` 收发 `HANDLE` 需与 `HGLOBAL` 互转；`CF_HDROP` 是 `CLIPBOARD_FORMAT(u16)` 要 `as u32`）、`windows/dwm.rs`（Phase 20：`disable_nc_rendering`——DWMWA_NCRENDERING_POLICY → DWMNCRP_DISABLED 属性级禁 DWM 非客户区渲染；`apply_rounded_region`——SetWindowRgn + CreateRoundRectRgn 圆角裁剪，半径 = CSS 8 逻辑像素 × DPI/96；Win10 透明圆角浮层直角阴影的修复，见「Surface 层级」条目）。
- `target_window.rs` — Phase 7 内存态 `TargetWindowState { Mutex<Option<TargetWindowInfo>> }`。记录快捷搜索打开时的前台窗口。按会话作用域：每次打开 `capture_from_foreground`（泛型 `<R: Runtime>`）先清空再写入，`peek` 在同一会话内复用，`clear` 在隐藏 / 粘贴失败时调用，60 秒 TTL。绝不持久化。
- `selection_capture.rs` — **Phase 15 选中文字搜索的编排层（替换语义：取词即剪切）**。`SelectionSearchState`（开关内存镜像，默认 `true`，前端推送；`explicitly_set` 标志区分默认值与显式设置）+ `capture_selected_text(app)`：开关关 → 直接 `None`（完全不动剪贴板与输入框）；开 → 先 UIA（`selection_reader::read_selection_uia`，读到后 `cut_selection` 补 Ctrl+X 删选区），失败走 `ctrl_x_fallback`（**等修饰键物理松开**最多 600ms → `send_ctrl_x` 取词+删除一步完成 → 轮询 `read_text` 变化最多 ~300ms；剪切文字**留在剪贴板**作为放弃选择时的找回途径，不恢复原剪贴板）。`sanitize_selection`：trim、折叠空白、40 字符截断（`chars().take`，CJK 安全）。选中文字只存在内存、绝不持久化。
- `shortcut_registry.rs` — 多拥有者（QuickSearch、ClipboardCollect）共享的全局快捷键注册。单个 `Mutex<HashMap<String, ShortcutOwner>>` + `ShortcutSyncState`（Unknown / Synced / RecoveryRequired）。`try_set` 是唯一变更点：先注册新快捷键，再注销旧的，回滚也失败则置 `RecoveryRequired`。冲突检测在 map 层（不经插件错误）。启动时调用 `reconcile` 清掉残留的 OS 级注册。`run_owner_action` 的 QuickSearch 分支直接调 `quick_search::show_quick_search`（Phase 15 统一，勿再内联复制 show 逻辑）。
- `database/` — 在 `app_data_dir` 打开 `emobox.sqlite3`，运行 `src-tauri/migrations/*.sql` 的迁移。给仓库暴露 `connect()`。**`configure_connection` 对每个连接设置**：`PRAGMA foreign_keys=ON`（**这是迁移里 `ON DELETE CASCADE` 真正生效的前提**——分组/标签软删不触发，只有物理 `DELETE` 触发）、`journal_mode=WAL`（读到的是连接自己的快照，跨语句可能看到不同状态，排查并发写入时要开事务取一致快照）、`busy_timeout=5s`、`synchronous=NORMAL`。**有七个迁移**：`0001_create_emojis.sql`（旧 17 列）、`0002_create_groups_tags.sql`（4 张新表：`groups` / `tags` / `emoji_groups` / `emoji_tags`，外键均 `ON DELETE CASCADE`）、`0003_add_emoji_trash_columns.sql`（`emojis` + `deleted_at` / `trash_path` / `trash_thumbnail_path` + `idx_emojis_is_deleted_deleted_at` 索引）、`0004_remove_external_directory_add_perceptual_hash.sql`（Phase 8：删除全部 `external_directory` 行 + 新增 `perceptual_hash` 列；**不建该列的 B-tree 索引**——去重是 Rust 全表 Hamming 扫描，普通索引无加速）、`0005_add_updated_at.sql`（Phase 11：`emojis` + `updated_at` 列，记录**元数据最后修改时间**，存量初始化为 `imported_at`）、`0006_add_group_pinned.sql`（Phase 13：`groups` + `is_pinned` 列，`DEFAULT 0` 自动回填存量；不建索引——分组几十行无加速）、`0007_add_group_icon.sql`（分组图标改造：`groups` + 可空 `icon` 列，NULL = 默认文件夹；存前端 `groupIcons.ts` 注册表里的图标名）。`run_migrations` 之后 `DatabaseState::initialize` 还会调用 `ensure_updated_at_column`——兼容早期把 5 号迁移应用成 `file_modified` 的库，按 `PRAGMA table_info` 幂等补列（不依赖 `ADD COLUMN IF NOT EXISTS`）。`connect()` 总是开新连接 —— 没有连接池。测试里的 `Connection::open_in_memory()` 不走 `configure_connection`（无外键/WAL pragma）。
- `repositories/emoji_repository.rs` — `emojis` 表的 SQL 访问。Phase 6 新增：`set_favorite_for_ids`（单事务批量）、`add_to_group` / `remove_from_group`（矩阵写）、`add_tags` / `remove_tags`（矩阵）、`record_image_used`（最近使用 bump）、`mark_deleted` / `set_trash_paths` / `clear_trash` / `delete_permanently` / `list_deleted_targets`（回收站状态机）、`list_indexed(options, query, tag_ids)`（统一搜索，Phase 8 起锁步绑定 + `组*标签` 精确语法；**Phase 17 起返回 `SearchPage { items, total }`**——回退级判定由 `resolve_search_mode` 用 COUNT 探测（offset=0 语义，与请求 offset 无关，否则翻页会错误回退到 PlainLike），锁步 WHERE 抽成 `build_search_where` 供 `list_indexed_impl` 与 `count_indexed` 共用；ORDER BY 新增 `name-asc`/`name-desc`/`format`/`added-time`/`modified-time` 五个分支且**每个分支都带 `e.id` 决胜列**——offset 分页要求全序确定）、`list_deleted(limit, offset)`（Phase 17 起分页 + total；路径用三参数 `COALESCE(trash_path, managed_path, source_path)`，`limit: 0` = 纯计数）、`search_recent`、`fill_relations`（两查询批量查 `emoji_groups` + `emoji_tags` —— 从无 N+1）、`get_relations_for_ids`（底层辅助）。**Phase 8 新增**：`find_duplicate_content(sha256, perceptual_hash, threshold, skip_perceptual_dedup)`（SHA 精确 + dHash 感知双通道，候选按 (Hamming, id) 升序取最优）、`find_by_id`、`list_null_perceptual` / `update_perceptual_hash`（惰性回填）、`insert_managed` 增 `ImportGroup` 同事务建组并返回 `InsertResult { emoji_id, group_id, group_created }`、`import_legacy_recent` 重写为只更新既有受管行（不再建 external 行）。**Phase 10 新增**：`parse_exact_query` 支持 `组*标签`（`:` 别名）+ `SearchMode{Exact, Lenient, FuzzyGroup, PlainLike}` 四级回退、`list_untagged_emojis`（文件名标签回填候选）。**Phase 11 新增**：`touch_updated_at(connection, ids)`（刷新 `updated_at` 修改时间；用户 id 级元数据操作在命令层调用，重命名/删除组、标签在仓库内部连带刷新成员，见 `docs/phase11-ui-polish-time-sort.md`）。
- `repositories/group_repository.rs` — `groups` 的 CRUD，加一个 `list_groups` 用子查询带每分组表情数。名称 `COLLATE NOCASE` 唯一。**Phase 13 新增** `set_group_pinned`（`is_pinned` 置顶，不刷成员 `updated_at`——与 rename/delete 区分，置顶只是侧栏排序）；`list_groups` ORDER BY 为 `is_pinned DESC, sort_order, id`（置顶组在前）。**分组图标改造新增** `set_group_icon`（`icon` 可空文本列，空串/空白归一为 NULL；不刷成员 `updated_at`、不发 notify，同 `set_group_pinned`）。`GroupRow` 含 `icon: Option<String>`。
- `repositories/tag_repository.rs` — 与 groups 同形，但针对 `tags`。名称同样 `COLLATE NOCASE` 唯一。
- `services/` — `asset_service`（在资源管理器中打开、临时文件 + 原子重命名、`encode_image_as_png` 是**全仓库唯一** PNG 编码点、`stage_file` / `stage_dynamic_image` / **Phase 16 `stage_bytes`**（内存字节 → 临时文件 → 与 `stage_file` 共用抽出的 `stage_temporary`：解码 → dHash → `animation_status` → Static>512px 才缩放重编码）/ `is_gif_bytes`（GIF magic 校验，剪贴板读写共用）/ `decode_for_import` / `animation_status` / `encode_scaled_image`；Phase 8 起受管副本静态图 >512px 缩到 512px 内重编码，动画/未知格式保持原字节）、`import_service`（`import_one` / `import_folder` / `import_dynamic_image` / **Phase 16 `import_bytes`**（剪贴板 `"image/gif"` 原始字节，SHA 对原始字节算）共用 `commit_staged_as_source_type`；`static IMPORT_LOCK: Mutex<()>` 串行化所有写入；`find_duplicate_content` 双通道去重 + `backfill_perceptual_hashes` 惰性回填 + `ImportGroup` 同事务建组；DB 失败回滚；**导入自动打文件名标签** —— `commit_staged_as_source_type` 对非 clipboard 来源用完整文件名 `find_or_create_id` + `add_tags`，失败仅 warn 不失败导入；另有 `ImportService::backfill_filename_tags` 在 `lib.rs::setup` 启动时一次性回填存量无标签表情，幂等）、以及 **`trash_service`**（Phase 6 新增的唯一服务）。`trash_service` 是 `soft_delete` / `restore` / `permanently_delete` / `empty_trash` 的 FS↔DB 编排器。它强制 **"先移文件再写 DB"** 不变量：主文件用 `fs::rename`（跨卷用 `fs::copy + fs::remove_file` 回退），缩略图只在主文件成功后移动，然后单个 DB 事务。主文件失败会把该行 `is_deleted` 回滚为 `0`。Group / Tag 的 CRUD 留在各自仓库 —— 无需服务层。Phase 7 新增 **`chat_paste_service`** —— 编排 `paste_to_target_window`：`validate` → `activate` → `restore_input_focus` → `SendInput` Ctrl+V。从不返回 `Err`；每个失败都映射为可序列化的 `PasteResult { kind, reason, processName, message }`（`kind` 是 camelCase：`success` / `clipboardOnly` / `disabled`）。`commands::paste_to_target_window` 是唯一调用者：它 `peek` 目标（不消耗 —— 同会话复用），失败时 `clear`。

## 三种导入模式（承重区别）

> **Phase 8（索引移除）**：外部目录"仅索引原路径"模式已整个删除（`scan_directory` / `ScanSummary` / `upsert_external_scan` / `get_indexed_images` 不再存在，存量 `external_directory` 行由 migration 0004 删除）。任何导入都是复制进受管库。

| 模式 | Tauri 命令 | 行为 | 持久化？ |
|---|---|---|---|
| **导入图片 / 拖放（受管复制）** | `import_managed_paths`（`import_service`） | 把选中的文件复制到 `app_data_dir/assets/emojis/`，写缩略图，存 `managed_path` + `sha256` | 是（SQLite + 文件系统） |
| **导入文件夹（受管复制 + 自动分组）** | `import_folder`（`import_service::import_folder`） | 递归复制文件夹内所有支持图片；**每个顶层子文件夹自动建同名分组**（懒建：仅当该子文件夹第一张图成功导入才建组，失败/重复不建空组）；**平铺文件夹（无任何子文件夹）按文件夹本身建同名分组**；有子文件夹时根目录散图不归组 | 是（SQLite + 文件系统） |
| **剪贴板收藏（受管复制，用户触发）** | `collect_image_from_clipboard`（`clipboard_collect`） | **Phase 16**：按序尝试动画保真通道 —— ①`"image/gif"` 原始字节（Firefox）②`CF_HDROP` 文件路径（QQ 复制图片/资源管理器复制图片文件，只读源文件；GIF 保动画，**2026-09 起非 GIF 受支持静态图也走此通道按原始字节导入[通道 0.1]**）③网页 GIF URL 联网下载（设置开关 `downloadWebGif`，默认关）→ `import_bytes` 保留动画；都不可用 → `read_image()` 读 RGBA，经 `stage_dynamic_image` 重新编码为静态 PNG（检测到网页动图时 toast 提醒）。SHA-256 去重（gif 路径对原始字节算），以 `source_type='clipboard'` 存储。由 `Ctrl+Alt+S` 或菜单触发 | 是（SQLite + 文件系统） |

**去重（Phase 8）**：双通道 —— SHA-256 字节级（`DedupHitKind::ExactSha`，直接跳过）+ dHash 感知（`PerceptualDuplicate`，携带候选 id/路径/Hamming 距离，前端可"强制导入"）。导入命令带 `skip_perceptual_dedup` 参数，为 true 时只绕过感知、不绕过 SHA。迁移 0004 后旧受管行 `perceptual_hash` 为 NULL，由 `import_folder`/`import_paths` 触发**惰性回填**（`ImportService::backfill_perceptual_hashes`，每批 ≤50）。

**压缩（Phase 8）**：受管副本静态图（PNG/JPG/WebP）任一边 >512px 缩到 512px 内再显式重编码（PNG Fast / JPEG q85 / WebP lossless）；GIF / APNG / 动画 WebP / 无法确认格式一律保持原始字节（`AssetService::animation_status` 内容级检测）。dHash 在压缩前对解码图计算（EXIF 方向已应用）。

**Phase 22：导入自动加入当前分组**：`import_managed_paths` / `import_folder` / `collect_image_from_clipboard` 均接受 `target_group_id: Option<i64>`（前端 camelCase `targetGroupId`），service 层经 `import_group_for_target` 映射为 `ImportGroup::Existing(id)`、归组发生在 `insert_managed` 同一事务内。`import_folder` 带 target 时**抑制**子文件夹/平铺自动建组——全部图片（含根散图）只归入目标分组，`groups_created` 为空；不带 target 时行为不变。重复（SHA/感知）不导入也就不入组，「感知强制导入」重试同样入组。详见 `docs/phase22-import-into-current-group.md`。

`EmojiLibraryView` 消费 `filteredItems`（`App.tsx` 投影自 `currentEmojis`，即**当前视图已加载页**）并投影为 `IndexedImage`（Phase 8 起 7 字段，含 `id`）供网格用。`App.tsx` 里按视图的 useEffect 选择正确的后端端点：四个列表视图（`all` / `favorites` / `ungrouped` / `group:N`）用 `search_emojis`（Phase 17 起带 `sort` + `limit: 200, offset` 分页），`trash` 用 `list_deleted_emojis`（同样分页），`recent` 用本地 `recentItems`（客户端过滤 + 客户端排序，唯一保留客户端排序的视图）。**排序（Phase 17）全部在服务端**——客户端只排序 recent 视图；切换排序 = effect deps 含 `sortOption` 重拉第 1 页。搜索框支持 `组*标签` 精确语法（`list_indexed` 后端解析，NOCASE，精确空结果依次回退「组精确+标签 LIKE」→「组名子串+标签 LIKE」→ 普通 LIKE；也支持 `组名*` / `*标签` / 全角 `＊`，`:` / `：` 保留为别名）。recent 视图是客户端过滤（`src/lib/searchSyntax.ts` 镜像同一语法，含模糊组名回退），经 `RecentImageRecord.groupIds/tagIds`（后端 `fill_relations_for_recent` 填充）做组/标签匹配。**网格选中与右键（Phase 9，Phase 18 改单击语义）**：`EmojiGrid` 持有一个共享受控 `Menu`（`EmojiItemMenu` 内容），用 `positioning={{ target }}` 光标定位（本地 `VirtualTarget` helper，事件/按钮 rect 两种锚点，不依赖 `usePositioningMouseTarget`；`handleContextItem` / `handleMoreButton` 经 `openMenuForRef` latest-ref 保持身份稳定——memo 前提）；`EmojiGridItem` 普通单击**不再是选中而是复制**（250ms 消歧，见 Phase 18），Ctrl/Shift 仍切换/范围，多选模式下单击切换选中；`EmojiItemMenu` 的 `multi` 为 true 时隐藏 复制/查看文件位置；**trash 模式整个不渲染 复制/查看文件位置**——回收站只允许 恢复 / 彻底删除（2026-09 收紧：此前回收站里还能复制、查看文件位置，属越权操作；同月收紧扩到导入——工具栏「导入」按钮 `showImport={currentView !== "trash"}` 整体隐藏、空回收站是专属空状态「回收站是空的」不带导入按钮、三条视图绑定导入入口读 `currentViewRef` 拒绝）。选中 ≥2（多选模式下 ≥1）时 `EmojiLibraryView` 在 `.content` 底部浮出批量条（`LibraryHeader` 提供「多选」开关与 Phase 17「全选/取消全选」按钮；密度切换图标三档区分：紧凑 `GridDots20Regular` / 标准 `Grid20Regular`+Filled / 宽松 `Apps20Regular`+Filled）。

## 快捷搜索浮层

- **Phase 20 重构**：`useQuickSearchQuery(activationId, reloadToken, groupId)` 驱动，每页 20 条（`limit/offset` 下推 + `SearchResult.total` 驱动 hasMore，「加载更多」追加，offset 按服务端返回行数前进、全被去重的页停止防死循环）。**搜索语义（用户指定）**：非空 query → **全库搜索**（`view:"all" + query`，支持 `组*标签`，分组筛选挂起——找表情时用户不记得它在哪个分组，不做分组内搜索）；空 query + 分组 → 浏览该分组（`view:"group"`，最近优先）；空 query + 全部 → 全库最近优先。结果按 key（分组+关键词）缓存在 hook 内 Map：来回切秒回不重拉、跨唤起存活、`reloadToken`（library-changed）变化整体失效；`requestSeq` 守卫覆盖 loadMore 在途响应，`loadingMore` 无守卫恢复防按钮锁死。点分组按钮 = 清空关键词切回分组浏览（对称交互）；每次唤起重置回「全部」。监听 `quick-search-opened` 触发重载（Phase 15 起 payload 携带 `selectedText`，非空作为 seed query 注入）。详见 `docs/phase20-quick-search-refactor.md`。
- 数据变更命令（导入 / 删除 / 收藏 / 分组 / 标签）成功后 `quick_search::notify_library_changed` 向浮层发 `library-changed`，浮层收到后重载当前搜索（不重置输入，缓存整体作废）。
- **布局与分组入口（Phase 20）**：内容区 = 搜索框（放大：内层 input `"& input"` 提 `fontSizeBase400`，且 `maxWidth:"none"`——SearchBox 自带 max-width 468px 会把 width:100% 截断贴左）→ 置顶分组行（`listGroups()` 过滤 isPinned，**唤起时刷新**——置顶是纯侧栏变更不发 library-changed；固定 36px 横向滚动不撑高）→ 状态行（搜索/分组/最近使用 · 计数）→ 结果网格（`img` lazy + 失败占位）+「加载更多」（`tabIndex={-1}` 防 Enter 冲突）→ footer `↑↓ 选择 · Enter 复制 · Esc 关闭`。
- **失焦关闭（Phase 20）**：`getCurrentWindow().onFocusChanged` blur → close，双守卫：激活后 300ms 内的 blur 忽略（show/center/set_focus 序列的瞬时事件）、`overlayDragGuard`（拖拽 move loop 会让窗口短暂失焦，不抑制则按住浮层一拖就消失；focus=true 清标志 + 3s 超时兜底）。close 只调 `hideQuickSearch`，**绝不清 TargetWindowState**。
- **整窗拖拽（Phase 20）**：`QuickSearchPanel` 根节点 onMouseDown → `startDragging()`（capability `core:window:allow-start-dragging`），按住任意背景可拖；排除 `button/input/[role='option']/[role='search']/[data-no-window-drag]`，结果滚动区标 `data-no-window-drag` 保住滚动条。`show_quick_search` 的 `center()` 仅首次显示执行（拖拽位置跨唤起保留，重启重置）。
- Enter / 鼠标点击触发 `copyImageToClipboard`（Rust 命令），成功/失败弹 toast，成功复制后约 500ms 自动隐藏浮层。失败时浮层保持打开，错误内联显示。
- 默认全局快捷键是 `Ctrl+Alt+Space`（不是 `Alt+Space`，Windows 系统窗口菜单保留了它）。`src/config/shortcuts.ts` 导出两个常量以及 `ShortcutEditor` 用的 `shortcutFromKeyboardEvent` 辅助函数。

## 托盘菜单窗口（Phase 26）

原生 Win32 托盘菜单（旧 `MenuItem` 三项）是系统级控件，字体/尺寸/配色不可定制（用户反馈「丑、太小」也无法加「设置」）——**不要试图给原生菜单做样式**，Phase 26 已整体换成自绘 Fluent 菜单窗口，详见 `docs/phase26-tray-menu-window.md`。

- **交互**：左键单击托盘 = 打开主窗口；右键单击 = 图标上方弹出 `tray-menu` 窗口（四项：打开主窗口 / 打开搜索浮层 / 设置 / 分隔线 / 退出）。菜单关闭 = 失焦 / Esc / 点击任意项。
- **定位**：`show_tray_menu(app, rect)` 按托盘事件 `rect`（物理像素）算位置——右缘对齐图标、底边悬上方 8px，再按图标所在显示器 `scale_factor` 换算窗口物理尺寸并 clamp 在显示器内（窗口首次显示前 DPI 归属不定，**不能依赖 `outer_size()`**；`TrayIconEvent.rect` 的 position/size 是 Logical|Physical 枚举且 Physical 分支是 i32/u32 整型）。`set_size`/`set_position` 后必须**重算 OS 圆角区域**（`SetWindowRgn` 不随窗口 resize 自动更新，每次弹出前重新 `apply_rounded_region`，否则角部裁剪与实际窗口错位）。
- **先藏后动作（承重顺序）**：`handle_menu_action` 统一先 `hide_tray_menu` 再执行动作——alwaysOnTop 弹窗不先藏会阻塞聚焦；`open-search` 更必须先藏再 `sleep 50ms`，否则 `show_quick_search` 的 `capture_from_foreground` 会**把托盘菜单自己抓成自动粘贴目标**。前端点击菜单项也是先本地 `hide()` 再发 `trayMenuAction` IPC（双保险，重复 hide 无害）。
- **设置入口**：菜单「设置」= `show_main_window` + `emit_to("main", "settings-open-requested")` → `App.tsx` 独立 effect 监听（同 `main-close-requested` 模式：handler 只 setState、注册一次）→ `setSettingsOpen(true)`。主窗口 webview 隐藏时依然存活（Phase 25 已依赖此事实），事件必然可达。
- **前端结构**：`src/features/tray-menu/`（`TrayMenuPanel` + `TrayMenuWindow`）。窗口常驻隐藏不销毁（同浮层）；失焦关闭带 300ms 激活守卫 + `hideRef` latest-ref（无拖拽所以不需要 `overlayDragGuard`）；监听 `tray-menu-opened` 重置激活时间 + `activationId` 递增 → `key` 重挂面板重播 `FadeSnappy appear` 入场动画（仅容器级）。**surface 必须铺满整个窗口（width/height 100%）、窗口高度贴合内容（248×162）**——首版 surface 内容自适应 + 窗口留透明余量，底部踩出一条 WebView2 白条（WebView2 对透明窗口底边不合成透明度、露出默认白底，即 Phase 20 的「底边透明残片」；快捷搜索浮层没暴露是因为 surface 铺满 100%），**透明窗口内部绝不能留 CSS 透明条带**；菜单项 36px 自定义 `<button role="menuitem">`，「退出」前 Fluent `Divider` 必须 `flexGrow: 0` + 手动收紧 margin。
- **新透明窗口接入清单**：`tauri.conf.json` 加窗口定义（transparent/shadow:false/decorations:false/alwaysOnTop/skipTaskbar/visible:false）→ `capabilities/default.json` 的 `windows` 数组加 label → `lib.rs` 的 `apply_rounded_overlay_style` 循环与 `CloseRequested` 隐藏分支加 label → `main.tsx` 挂组件 + `<html>` 类名 → `global.css` 透明规则加类名。

## Phase 7：自动粘贴到打开浮层前的窗口

用户在输入窗口（微信 / QQ / 飞书 / 任何应用）里按快捷键、选表情，EmoBox 复制到剪贴板、恢复打开浮层前的窗口、合成 `Ctrl+V`。**它绝不发送 Enter。**

流程：`show_quick_search` 捕获前台窗口 → 用户选择 → `copyImageToClipboard` → `hideQuickSearch`（必须*在*粘贴之前隐藏 —— always-on-top 浮层会阻塞 `SetForegroundWindow`）→ 50ms 稳定 → `paste_to_target_window` → toast。

- **`autoPaste` 设置**（默认 `true`，非 Windows 默认空操作）：`ThemeProvider.tsx` 里的 `PersistedSettings.autoPaste`，持久化到 `localStorage: emobox.settings`；开关在 `SettingsMenu.tsx`「常规」。
- **前端调用链** `QuickSearchWindow.tsx::copySelectedImage`：复制 → 500ms 关闭计时 → 复制成功 toast → 若 `autoPaste`：`await hideQuickSearch()`（失败 → 降级 toast，不再粘贴）→ 50ms → `await pasteToTargetWindow()`（IPC 失败 → 降级 toast）。`copyingPath` 在所有路径恢复，避免后续选择被锁死。
- **`PasteResult` 契约**（Rust ↔ TS）：`kind` ∈ `success` / `clipboardOnly` / `disabled`（camelCase）；`reason` ∈ `noTarget` / `targetClosed` / `pidMismatch` / `activationFailed` / `inputFailed` / `invisible` / `ipcFailed` / `hideFailed`；`processName: string | null`。前端把 `reason` 映射为具体的 toast。
- **生命周期**：每次打开先清空再写入（跨会话复用不可能）；`peek`（不消耗）让同一会话可反复粘贴；粘贴失败时命令层 `clear`；**浮层隐藏不 clear**（hide-then-paste 顺序，Phase 15 修复）；60 秒 TTL 兜底。
- **关键 Win32 不变量**：绝不含 `VK_RETURN`；`AttachThreadInput` 所有路径 detach（Drop guard）；`SendInput` 拆成带间隔的逐事件调用；激活前复验 PID 防 HWND 复用；输入不经过 shell/`Command::new`；不用 IM 私有协议 / 不读内容 / 不后台轮询；HWND 绝不持久化。
- **已知缺口（Phase 15 已修复，待真机复验）**：`focus_restore.rs` 曾因 `VARIANT` 嵌套 union 构造在 Rust 2024 下撞 `E0133` 编译损坏，Phase 15 已按文档既定方案重写（`CreateTrueCondition()` + `FindAll` + 遍历 `CurrentControlType() == UIA_EditControlTypeId`）。Windows 构建已恢复，但真机自动粘贴端到端（尤其 Electron 应用 / 原生微信的 UIA 可见性）**尚未复验** —— Electron 应用需 Chromium 无障碍树就绪，原生微信靠 `EnumChildWindows` 回退。完整细节与决策记录：`docs/phase7-chat-paste.md` 与 `docs/phase15-selection-search.md`。

## 关键不变量 / 硬性规则

- 用户的原始图片绝不被 EmoBox 复制、移动、改名或删除，*除了* `import_managed_paths` 和 `import_dynamic_image` / `import_bytes`（剪贴板收藏，Phase 16 起含 GIF 原始字节路径）有意复制进受管素材目录，以及 **`trash_service::soft_delete` / `restore` / `permanently_delete` / `empty_trash` 在 `app_data_dir/assets/trash/` 内外移动文件**（注意 trash 在 `assets/` 下，不是 `app_data_dir/trash/`——`trash_service.rs` 由 `emojis_directory().parent().join("trash")` 推导）。读路径对剪贴板 `CF_HDROP` 指向的外部文件（如 QQ 原图缓存）**只读**、绝不改动。不要加任何其他会改动用户文件的路径。
- **主窗口与快捷搜索窗口的关闭（Phase 25 重写）**：`quick-search` 的 `CloseRequested` 永远拦截并隐藏；`main` 由 `CloseBehaviorState` 决定——`Some(true)` 隐藏驻留托盘、`Some(false)` `app.exit(0)` 整应用退出、`None`（未选择，默认）发 `main-close-requested` 事件给前端弹 `CloseActionDialog`（勾「记住」写回 `closeToTray` 设置项——**弹窗结果与设置开关是同一状态**，见 ThemeProvider 条目）。`app.exit(0)` 的合法出口：托盘「退出」菜单、询问弹窗选「直接退出」、已记住「直接退出」。
- `copy_image_to_clipboard` 经剪贴板插件写 `tauri::Image`（RGBA），不是文本/`file://` URL。WebP 转成静态图；GIF 缩到首帧，**Phase 16 起** Windows 上随后追加 CF_HDROP 文件列表（主通道，指向受管 `.gif`）+ `"image/gif"` 原始字节（辅通道）——文件列表追加成功 `animationPreserved=Some(true)`（微信/QQ 粘贴为动图），失败降级 `Some(false)`（toast「仅保留首帧」）。同一命令随后调用 `EmojiRepository::record_image_used` 保持 SQLite 同步。
- **回收站（Phase 6）**：`soft_delete` 和 `restore` 遵循 **"先移文件再写 DB"**。主文件用 `fs::rename`（跨卷回退到 `fs::copy + fs::remove_file`）；缩略图只在主文件成功后移动；DB 事务只在两个文件都落地后写 `is_deleted = 1` 和回收站路径。主文件失败把该行 `is_deleted` 回滚为 `0` 并保持原文件原位。缩略图移动失败不致命（记录日志 + 记入 `TrashFailure`）；主文件移动对该行致命。`is_managed_source` 守卫（只有 `managed_import` / `clipboard` 才做文件移动）保留为防御——遗留的 external 行已被 migration 0004 删除。**软删行的路径列是悬空引用**：`soft_delete` 只写 `trash_path` / `trash_thumbnail_path`，`thumbnail_path` / `managed_path` 保留旧值（文件已被移走）。因此 `load_thumbnail` 的查询必须回收站感知——`COALESCE(trash_thumbnail_path, thumbnail_path)` + `COALESCE(trash_path, managed_path, source_path)`（与 `list_deleted` 投影同序），**不要**加回 `WHERE is_deleted = 0`（曾导致回收站行重启后网格缩略图全部「预览不可用」——运行期被前端 `useThumbnail` 模块级内存缓存掩盖，重启才暴露）。
- **CASCADE 行为（Phase 6）**：`emoji_groups` / `emoji_tags` 上的 `ON DELETE CASCADE` 只在物理 `DELETE FROM emojis` 时触发。`UPDATE emojis SET is_deleted = 1` 是**软删**，*不*触发 CASCADE —— 软删行保留其分组/标签关联，恢复时继续生效。只有 `permanently_delete` 和 `empty_trash` 发出硬 `DELETE`，会原子地级联清掉关联。
- **路径语义（Phase 6）**：`IndexedEmoji.path` 是 SQL `COALESCE` 投影。`list_indexed` / `search` 用 `COALESCE(managed_path, source_path)`（2 参数）；`list_deleted` 用 `COALESCE(trash_path, managed_path, source_path)`（3 参数，`trash_path` 优先）。前端网格直接读 `path`，从不由自己拼。
- 最近列表：最多 50 条，按 `path` 去重，按 `lastUsedAt` 降序。**Phase 6 起 SQLite 是事实源** —— `last_used_at` / `usage_count` 在 `emojis` 上，`RecentImagesState` 是内存缓存，`recent-images.json` 文件只导入。
- 所有应跨重启持久化的 UI 状态都走 `ThemeProvider` / `localStorage: emobox.settings`（主题、侧栏折叠、分组区折叠、默认视图、两个全局快捷键、`autoPaste`、`selectionSearch`、`downloadWebGif`、Phase 27 的 `autoCheckUpdates` / `updateMirrors`）。不要引入其他持久化后端。（分组 / 标签 / 收藏状态**不**走这里 —— 它们经 Phase 6 命令存在 SQLite；分组自定义图标也存 SQLite（迁移 0007），**不**进 localStorage。Phase 7 的目标窗口**仅内存** —— 绝不持久化其 HWND。）
- **Phase 7 自动粘贴**是"先复制再输入模拟"流程，**不是**窗口消息粘贴。`paste_to_target_window` 从不返回 `Result::Err`；每个失败都是 `PasteResult::clipboardOnly`。目标窗口按会话作用域，在隐藏 / 失败 / TTL 时清空。`windows/` 模块是全 crate 唯一的 `unsafe`，藏在 `#[cfg(windows)]` 后面。UIA 条件查询一律用 `CreateTrueCondition` + `FindAll` + 遍历比对（不要用 `CreatePropertyCondition`/VARIANT —— edition 2024 下 E0133）。
- **选中文字搜索（Phase 15）**：**替换语义** —— 取词即剪切（选中文字从输入框删除，表情粘贴正好落在原文字位置；放弃选择时剪切文字留在剪贴板可手动找回）。选区文字只存在内存（事件 payload）、**绝不持久化**；开关关闭时 Rust 完全跳过（不动用户剪贴板与输入框）；任何读取失败 → 浮层空 query 正常打开，绝不阻塞浮层显示。合成 Ctrl+X 前必须等修饰键物理松开（最多 600ms，超时放弃）—— 不等会发出 Ctrl+Alt+X 或裸按键，裸按键会替换选区误删用户文字（真机复现过）。`send_ctrl_x` 与 `send_ctrl_v` 一样绝不含 `VK_RETURN`。详见 `docs/phase15-selection-search.md`。
- **GIF 动画（Phase 16）**：展示层悬停播放 —— `.gif` 项的 asset URL 走 `emojiAssetUrl`（scope：`$APPDATA/assets/emojis/**` + `$APPDATA/assets/trash/**`），加载失败回落静态缩略图且实例内不重试；受管 GIF 是**原始字节**（未压缩），不要做全量预载。剪贴板两条链路都必须**静默降级**：读路径三条动画通道（`"image/gif"` 字节 → `CF_HDROP` 文件路径 → 网页 URL 联网下载（需 `downloadWebGif` 开关））都读不到（Chrome/Edge 且开关关闭即此情形，剪贴板上只有 DIB+网页 URL）→ RGBA 首帧路径；写路径追加失败 → warn + `Some(false)`，绝不让复制失败。**粘贴端（微信/QQ）不消费 `"image/gif"` 位图格式**——它们粘贴动图靠 **CF_HDROP 文件列表**（资源管理器 Ctrl+C 同款），写路径主通道是 `write_file_drop`（指向受管 `.gif`）。**读端 QQ 复制聊天图片带 CF_HDROP 本地路径**（指向 `nt_qq\...\Ori\*.gif` 原图）——读路径用它免联网拿动图，**只读源文件、绝不改动**。Win32 剪贴板所有权规则承重：`GetClipboardData` 的 HGLOBAL 属系统**绝不 free**；`SetClipboardData` 成功后所有权转移**不得 free**、失败路径才 `GlobalFree`；追加格式**不 `EmptyClipboard`**（保留插件写的 DIB/PNG）。`OpenClipboard` 失败要重试（5×10ms，剪贴板可能被剪贴板工具持续占用——开发机上见过整机会话被占死、连 `Set-Clipboard` 都失败的情形，probe 记录见 phase16 文档）。详见 `docs/phase16-gif-animation.md`。
- **网格多选（Phase 9）**：`selectedIds` 是唯一选中源，由 `useMultiSelection(filteredItems)` 托管；prune 按 id 集合并行（排序变化不清、换视图/搜索收窄跟随可见集收缩）。批量 id 直接用视图项 `IndexedImage.id`（各视图含 recent 都真实）。批删/恢复/彻底删除后要同步剪 `currentEmojis` / `recentItems` / `favoriteIds` 并 `deselect`、`viewTotal` 递减（Phase 17 起 `indexedEmojis` / `allItems` / `favorites` 三个 state 已删，计数走后端 total）；**收藏视图里取消收藏同样本地剪辑**（2026-09 修复：`toggleFavorite` 在命令成功后若 `currentViewRef.current === "favorites"` 就过滤 + `viewTotal` 递减 + `deselect`——否则条目留到下次切视图才消失；判定必须走 ref 读实时视图，防 await 期间切视图误递减）。切视图用 `prevViewRef` effect 统一清选区 + 关多选模式，**不要**在视图 effect（deps 含 `debouncedQuery` / `recentItems`）里清。键盘 Ctrl+A / Delete 走 `keyShortcutRef` latest-ref，输入框与模态弹窗打开时豁免。
- **主窗口分页（Phase 17）**：`currentEmojis` 只持有已加载页（`PAGE_SIZE = 200`），总数一律看 `viewTotal` / 后端 `total`（header「共 N」、侧栏三计数、`hasMore`），**绝不用数组长度当总数**。`loadMore` 的 offset 用 `nextOffsetRef`（按服务端返回行数前进，**可空**：视图 effect 开头置 null、第 1 页落地才赋值，null 时 `loadMore` 直接返回——keep-previous 窗口期旧内容还在显示，哨兵若触发会拿旧 offset 给新视图拉第 2 页），不能用 `currentEmojis.length`——本地删除/去重让两者错位后，全被去重的页会永远请求同一 offset 死循环；追加按 id 去重防 offset 漂移；迟到响应按 `viewSeqRef` 丢弃。**keep-previous 切视图（2026-09 修复闪屏）**：拉取期间**不清** `currentEmojis`（旧内容无动画保留），新数据落地时经 `viewGeneration`（落地代数 state，只在 view/query/sort 真正切换且落地时递增，同 key 重拉如 recent 复制/分组改名不递增不重播动画）驱动 `EmojiLibraryView` 的入场动画 key 与 `ready` prop（落地前渲染 null、不闪空状态）、`contentRef` 滚动回顶——header 计数与内容同一次 commit 原子更新。排序在服务端（`SearchOptions.sort` 7 字面量），每个 ORDER BY 分支必须带 `e.id` 决胜列（offset 分页要求全序确定）。搜索回退级判定（`resolve_search_mode`）用 COUNT 探测、与 offset 无关——按当页结果判空会让第 2 页错误回退到 PlainLike。「全选」只选已加载项（复用 `selectAll()`），不要让它触发拉全量。recent 视图不分页（50 上限、客户端过滤排序）。快捷搜索浮层 Phase 20 起同样分页（每页 20 + hook 内结果缓存，见「快捷搜索浮层」），它与主窗口共用 `search_emojis`——改返回契约时两边都要动。详见 `docs/phase17-pagination-selectall.md`。
- **导入自动加入当前分组（Phase 22）**：目标分组在**发起导入那一刻**捕获（`getCurrentGroupId()` 读 `currentViewRef`，不读 state——拖放 effect 与 `collectFromClipboardRef` 不随视图重注册，读 state 会闭包过期）。全局快捷键收藏归属主窗口当前浏览的分组（后台窗口时亦然）。导入完成停留语义：`prepareAfterImport(targetGroupId)` 仅当「targetGroupId 非空 **且** 用户仍停在 `group:<id>` 视图」时留组重拉，否则维持切回「全部」；同视图重拉必须走 `viewReloadTick`（新增 deps 时别忘了 landingKey 判定——同 key 不递增 viewGeneration、不重播入场动画）。分组在导入期间被删：service 层 `ensure_target_group_exists` 在批量开始前整批报错、零半成品，**不要**依赖 FK 错误逐张失败。详见 `docs/phase22-import-into-current-group.md`。
- **导入加载条（Phase 23）**：四条导入入口（导入图片/导入文件夹/拖放/剪贴板收藏）共用 `useLibraryImport` 的 `isImporting`（`EmojiLibraryView` 状态行的 indeterminate `ProgressBar` + 「正在导入表情…」）。加载条**结束后延迟 600ms 才熄灭**（模块常量 `IMPORT_INDICATOR_MIN_VISIBLE_MS`）——拖几张图/收藏单张这类短导入几百毫秒就完，即时熄灭用户感知不到（真机反馈）；并发导入按 `activeImportsRef` 计数托管，最后一个结束才计时。**新导入入口必须走 hook 的 `beginImport`/`endImport`，不要自己 set state**；`collectFromClipboard` 曾是唯一漏接的入口（Phase 23 补上——剪贴板命令返回 outcome 枚举而非 Result，靠 `finally` 熄灭，Empty/Duplicate/Failed/Unavailable 全部分支都覆盖）。连带：`isImporting` 期间 ImportMenu/空状态按钮禁用、App.tsx 拖放守卫忽略新 drop（含 600ms 余晖窗口）。测试锁在 `src/features/import/useLibraryImport.test.ts`（生命周期/失败路径/并发）。详见 `docs/phase23-import-loading-indicator.md`。
- **卡片交互与网格性能（Phase 18）**：普通单击 = 复制（**不选中**；选中入口 = Ctrl/Shift、右键 `openMenuFor` 的 replace 兜底、Enter/Space、多选模式、Ctrl+A），双击 = `EmojiPreviewDialog` 大图预览（左右布局：左图 `width/height auto` + 双 max 约束按比例缩放，右 232px 信息面板；App 持有 `previewItem`，预览打开时 `keyShortcutRef` 弹窗豁免要包含它）。单双击消歧只走 `useClickIntent`（250ms timer + `event.detail > 1` 拦截第二击；修饰键/多选模式点击同步执行不进延迟；慢速双击 250–500ms 会先复制再开预览，已知可接受）。**复制成功的 toast 双通道防双弹**：主窗口复制由 `handleCopy` 直接用命令返回的 `outcome.message` 弹（**不依赖 `image-copied` 事件链路**——长 dev 会话的 HMR 残留曾让监听失效、复制无任何反馈）；`copy_image_to_clipboard` 每次成功仍会 `emit_to("main")`，监听器负责快捷搜索浮层复制的主窗口报信 + `recentItems` 更新，并凭 `localCopyToastRef`（handleCopy 弹前打的标）跳过本窗口已弹过的同一次复制，另有 1.2s 同图 `lastCopyToastRef` 防重兜住重复投递。改这条链路时两个 ref 都要保留。**性能红线**：① 网格卡片上的常驻悬浮提示一律用原生 `title`，**不要**给 `EmojiGridItem` 挂 Fluent `Tooltip`/`HoverCard`（重型组件，200+ 卡 × N 实例曾让整网格卡死；悬浮按钮上的 3 个 Tooltip 是既有存量、也别再加）；② `EmojiGridItem` 是 `React.memo`，**传进卡片的所有 props 必须身份稳定**（App 侧回调 latest-ref / useCallback、投影与 tags 数组的 WeakMap 缓存、`EmojiGrid` 的 `EMPTY_TAGS` 模块常量）——加新 prop 时先想清楚它稳不稳，内联箭头函数/新建 `[]` 会让整网格 memo 失效；③ 若深滚动上千张仍卡，下一步才是列表虚拟化。**Fluent `DialogSurface` 陷阱**：默认 `position:fixed; inset:0; margin:auto; max-width:600px`，宽度**拉伸到 max-width 而非贴合内容**——要弹窗收缩贴合图片就显式 `width: "fit-content"`。预览文件名 `titleText` 是块级 + `overflowWrap: "anywhere"` 自动换行（2026-09 修复：截断三件套挂在 inline `<span>` 上无效——`whiteSpace: nowrap` 生效但 `overflow`/`text-overflow` 被忽略，长文件名单行溢出卡片；截断样式必须配块级元素）。详见 `docs/phase18-emoji-card-interaction.md`。
- **统一通知模型（2026-09 收敛）**：全局操作反馈（成功/提示/**失败**）一律走右上角 Toast——主窗口与浮层各挂一个 `Toaster`；主窗口失败统一经 `App.tsx::notifyError`（`intent: "error"` + `timeout: 7000`，默认 5s 对错误信息太短），浮层复制失败本来就走 toast。上下文局部反馈保留内联：弹窗表单错误（`GroupDialog` 的 error span、`MoveToGroupDialog`/`TagPickerDialog`/`GroupIconPickerDialog`/`ShortcutEditor` 的 MessageBar）与浮层 footer 的 `copyError` 状态行。**主窗口不再有顶部 MessageBar**——原 `LibraryMessage` + `EmojiLibraryView.error` 通道已移除，App 的 `setError` 全部换成 `notifyError`，`useLibraryImport(onError)` 回调式上抛、不再持有 error state。新增失败提示不要重建持久错误条，直接 `notifyError`。
- **App.tsx 事件监听的闭包陷阱**：`listen(...)` 事件回调注册在 deps 不含 handler 的 `useEffect` 里，会捕获首次挂载时的旧闭包——handler 依赖设置值（如 `downloadWebGif`）时改设置**不生效**（Phase 16 真机踩过：`Ctrl+Alt+S` 走 `clipboard-collect-requested` 监听，开关开了仍走旧值）。解法一律是 latest-ref 转发（`collectFromClipboardRef`，与 `keyShortcutRef` / `clearSelectionRef` 同模式），不要把 handler 塞进该 effect 的 deps（会连带重跑 refreshLibrary 等初始化逻辑）。
- **侧边栏（Phase 13 + 分组图标改造）**：分组列表是侧边栏唯一 `flex:1` 弹性/滚动区（`min-height:0` 必须有，否则 flex 不滚动）；其余固定项保持紧凑，不要再给它们 `flex` 或大留白。**「我的分组」区折叠**：`groupList` 容器折叠时也必须保持挂载（只清空内容）——卸载弹性区会让底部固定项重排；折叠状态 `sidebarGroupsCollapsed` 走 ThemeProvider 持久化，折叠时 `searchOpen` 强制关闭、搜索/新建按钮隐藏。置顶排序唯一入口是后端 `list_groups` 的 ORDER BY（`is_pinned DESC, sort_order, id`），前端不重排；`set_group_pinned` / `set_group_icon` 不刷成员 `updated_at`、不发 `notify_library_changed`（纯侧栏外观变更先例）。**分组自定义图标**：图标名白名单事实源是 `src/features/library/groupIcons.ts` 注册表（`getGroupIcon` 对 NULL/未知名一律回退 `Folder24Regular`）；SQLite `groups.icon` 只存注册名（≤64 字符），加图标只改注册表一个文件；选择器是 `GroupIconPickerDialog`（搜索按英文小写/中文 label/keywords 子串匹配）；新建分组弹窗的快捷图标行用 `POPULAR_GROUP_ICONS`，**默认项 + 该数组必须共 18 个**（9 列网格两整行，缺一多一都会错行）；Fluent `SearchBox` 根元素 inline-flex 不撑满（见 LibrarySidebar 条目的坑）。搜索框收起后过滤必须失效（`searchOpen` 参与过滤条件）。「图钉」已从注册表移除（2026-09：置顶语义只保留名称旁的 16px 小钉，避免用户选图钉做分组图标时一行出现大小两颗钉）。
- **固定尺寸 SVG 进溢出 flex 容器必须 `flexShrink: 0`（2026-09 真机踩坑）**：侧栏分组行的置顶 `Pin16Regular` 曾「忽大忽小」——根因不是多处渲染尺寸不同，而是名称旁的 `pinLabel` flex 容器在**长分组名溢出时默认收缩把固定 16px 的 SVG 压小**（名字越长压得越小，最长的行被压到约 8px）。修复是 `LibrarySidebar.tsx` 的 `pinIcon` 样式（`flexShrink: 0`）。通用规则：图标/固定尺寸元素直接放进可能溢出的 flex 容器（如「图标 + 文本」标签行）必须 `flexShrink: 0`，或放进固定宽度 grid 列；排查这类「同一组件渲染忽大忽小」的问题先用像素级截图测量确认真实渲染尺寸，不要先怀疑多处代码路径。
- **Fluent `MenuItem` secondaryContent 陷阱（2026-09）**：`secondaryContent` 容器 `line-height: 20px`，往里放 20px 的 inline SVG 图标（如对勾）会因**基线对齐**把行盒撑到 25px，所在行比无 secondaryContent 的行高 5px（主题菜单真机量过 37 vs 32）。图标要加 `style={{ display: "block" }}` 脱离基线。内置 `checkmark` slot 在菜单项左侧，只给选中项传会左侧错位——右侧对勾布局用 secondaryContent + display:block 即可。
- **Fluent `Divider` 陷阱（Phase 14）**：`@fluentui/react-divider@9.7.4` 的 root 默认样式带 `flex-grow: 1`（藏在 `node_modules` 生成的 CSS 里，不读组件库源码察觉不到）。flex column 中放 `<Divider>`，只要容器高度可能大于子内容总和（固定高度 / flex 伸展），就必须显式 `flexGrow: 0`，否则它会与 `flex: 1` 区平分剩余空间（侧栏 `divider` 样式即因此加 `flexGrow: 0` + `flexShrink: 0`）。内容自然高度的容器（如 `TagPickerDialog` / `MoveToGroupDialog`）无此问题。
- **Surface 层级（2026-09 主题 token override）**：暗色自底向上 = BG2（窗口底/设置弹窗灰场）→ BG1（顶部栏/侧栏/主内容区/设置卡片/快捷搜索浮层 surface）→ BG3（表情卡片，最亮层浮出）。侧栏与内容区**同层级**（BG1 + 1px `colorNeutralStroke2` hairline 分隔，Fluent 深色导航范式）——勿把侧栏改回 BG2（曾因此成为全窗口唯一黑条、看着割裂）。设置弹窗整体是**单一 BG2 场**（`SettingsMenu` 的 `surface` 显式覆盖了 DialogSurface 默认 BG1，标题带/导航/面板同色，只有卡片 BG1 浮出）；快捷搜索浮层是**透明圆角窗口**（2026-09 改造：`tauri.conf.json` 的 quick-search 开 `transparent: true`、关 `shadow`——不要原生/CSS 投影；`main.tsx` 给 `<html>` 挂 `quick-search-window` 类，`global.css` 据此把 html/body/#root 背景置透明；Phase 26 起托盘菜单窗口 `tray-menu` 同范式，挂 `tray-menu-window` 类）：BG1 surface 带 `borderRadiusXLarge` 圆角 + 1px 描边、**无外衬包裹层**直接铺满窗口，圆角外区域透出底层窗口。**Windows 10 坑（tauri#11321，Phase 20 定案）**：DWM 阴影在圆角外留直角色块，且**在窗口可见期间反复调 `set_shadow(false)`（tao 样式标志位 + SWP_FRAMECHANGED）会让色块从偶发变必现**——勿在 show/Focused 事件里清阴影。有效修复是 `lib.rs::setup` 三件套：`set_shadow(false)` + `dwm::disable_nc_rendering`（DWM 属性级）+ `dwm::apply_rounded_region`（OS 级圆角裁剪，区域外像素一律画不出，兼治 WebView2 底边透明残片）。卡片 1px 常驻边框来自 `cardStyles.ts::cardBorderResetStyle`（`colorNeutralStroke2`，选中环/hover 只换 border-color 不抖布局）。改层级观感去 ThemeProvider override token，勿在组件里写十六进制色（见 ThemeProvider 条目）。
- **UI 动效体系（Fluent motion）**：时长/曲线一律 token 化——Griffel 样式（CSS transition/animation）用 `tokens.durationXxx` / `tokens.curveXxx`（字符串，解析为 CSS 变量），presence 组件参数用 `motionTokens.durationXxx`（数字毫秒），均从 `@fluentui/react-components` 导入；禁止裸毫秒 / 裸 cubic-bezier。分层：微交互 100–150ms（`durationFaster`/`durationFast`）、容器入场 200ms（`durationNormal`；`Slide`/`Collapse` 默认曲线即 Fluent 官方 decel 入 / accel 出，直接用默认）、大容器 250ms+。只动 `opacity`/`transform`（唯一例外：侧栏折叠的单元素 `grid-template-columns` layout 过渡，`AppShell` 与 `AppToolbar` 首列必须同曲线同时长，别只改一处）。**入场动画只做容器层**（`key` 重挂载 + `<FadeSnappy visible appear>` 包一层普通 div），绝不做 per-item——EmojiGridItem memo 红线；滚动追加批次（每批 72 个）不得带动画；密度切换保持瞬变。Presence 组件来自 **`@fluentui/react-motion-components-preview`**（显式依赖，与 react-components 9.74.x 的传递依赖同源；只用 Fade/FadeSnappy/Slide/Collapse/ScaleSnappy 基元）：克隆唯一 child 直接施加 WAAPI 动效，**child 必须是 DOM 元素**（`EmojiGrid` 返回 Fragment，要垫 div）；`appear` 默认 false，挂载播放入场必须显式传；**初始不可见或退场后要消失的场景必须传 `unmountOnExit`**——presence 默认退场后 child 仍挂载（透明的 sticky 条会占位并挡点击，selectionBar/dropOverlay 即因此必须带）。弹窗一律「常挂载 + `open` prop」（勿用 `{state && <Dialog/>}` 条件挂载——瞬间卸载会截断 Dialog 内置 Scale+Fade 退场动画）；常挂载弹窗的 payload props 在 open 变 true 时**快照进本地 state**（GroupDialog 的 open 键控 effect 是模板），否则退场 ~300ms 内 props 闪回退值（分组名清空、计数归零）；`EmojiPreviewDialog` 用 lastItem ref + meta 快照同理由。react-motion 系自动尊重 `prefers-reduced-motion`（无需任何处理）；**自写 CSS transition（如侧栏 grid-template-columns）必须手动加 `@media (prefers-reduced-motion: reduce): { transitionProperty: "none" }` 豁免**。**per-item 微动画（如多选 checkbox）用 `<ScaleSnappy visible={条件} unmountOnExit>` 常挂载包裹**——`appear` 默认 false 使滚动追加的新卡片 mount 时不播动画（红线），`visible` 翻转才播；勿用 Griffel `keyframes`（`@griffel/react` 1.7.7 无此导出，真机报过 SyntaxError）。主题切换保持瞬变（有意决策：Fluent 无官方过渡方案）。排查项：Windows「显示动画效果」关闭时所有 Fluent 内置动效会静默跳过（`prefers-reduced-motion` 生效），不是 bug。
- Rust 侧把表情元数据持久化在 SQLite（`app_data_dir` 的 `emobox.sqlite3`）。无云端、无账号、无网络同步——**联网例外只有两处**：Phase 16 的「联网下载网页 GIF」设置开关（默认关）会下载用户刚复制的剪贴板 URL（仅 http(s) `.gif`、超时 15s、上限 20 MB、不上传任何数据），见 `clipboard_collect.rs::attempt_web_gif`；Phase 27 的检查更新/下载安装包（`updater.rs`：拉 `releases/latest/download/latest.json` 与 NSIS 安装包，走用户镜像列表 → 官方直连，安装包 SHA-256 校验通过才启动安装器）。
- 支持的图片扩展：`png`、`jpg`、`jpeg`、`gif`、`webp`。检查点集中在 `scanner::supported_extension`（Rust）和 `useLibraryImport.ts` 的 `imageFilters`（TS）。
- 未实现的功能必须在 UI 里可见地 `disabled`，不能静默假装成功。参见 `ImportMenu.tsx` 的模式。
- Rust 单元测试在 `#[cfg(test)]` 模块里（例如 `asset_service.rs`、`import_service.rs`、`scanner.rs`、`recent.rs`、`clipboard.rs`、`clipboard_collect.rs`、`shortcut_registry.rs`、`updater.rs`、`repositories/emoji_repository.rs`、`repositories/group_repository.rs`、`repositories/tag_repository.rs`、`services/trash_service.rs`、`perceptual_hash.rs`；`platform/windows/clipboard_raw.rs` 的真实剪贴板 roundtrip 测试标 `#[ignore]`，手动 `cargo test -- --ignored` 跑）。JS 层检查是 `npm run build`（tsc --noEmit + vite）+ `npx vitest run`（共 7 个测试文件：`src/lib/searchSyntax.test.ts` 搜索语法镜像、浮层 `useQuickSearchQuery` 乱序/分页/缓存（Phase 20）、`useMultiSelection` 多选、`useGifPreview` 悬停播放、`useClickIntent` 单双击消歧、`useLibraryImport` 导入加载条生命周期/失败路径/并发（Phase 23）、`src/lib/mirrorSources.test.ts` 镜像源规范化/校验（Phase 27））。

### PNG 编码是确定性的 —— 对剪贴板去重承重

`AssetService::encode_image_as_png` 用 `PngEncoder::new_with_quality(CompressionType::Fast, FilterType::Adaptive)` + `ExtendedColorType::Rgba8` —— 全仓库**唯一** PNG 编码点。同样的 RGBA 输入总是产出字节级一致的输出和相同的 SHA-256，由 `deterministic_png_encoding_produces_identical_bytes_and_hash` 锁定。没有这个保证，反复从剪贴板收藏同一张图会撞不上 SHA-256 去重并产生重复文件。压缩级别取 Fast（受管副本已缩到 ≤512px，磁盘膨胀有界），确定性不受影响。

### D2 —— 剪贴板错误分类靠 arboard 错误文本

`tauri-plugin-clipboard-manager` 把 arboard 的类型化错误压平成 `String`，所以没法 `match` 错误变体。`clipboard_collect.rs` 匹配两个子串把"剪贴板没有图片"映射到 `Empty` 结果（info toast「剪贴板中没有图片」）：

- `"clipboard is empty"` —— 剪贴板真的空
- `"not available in the requested format"` —— 剪贴板是文本或其他非图片格式

其余都归 `Unavailable`（红色错误 toast）。这依赖 arboard 0.x 的 Windows 错误文本；升级可能需要重新验证。决策记录见 `docs/phase5-clipboard-probe-results.md`。

### `IMPORT_LOCK` 串行化所有写入

`import_service.rs` 持有 `static IMPORT_LOCK: Mutex<()>`，在 `import_paths`、`import_folder`、`import_dynamic_image` 顶部经 `lock_import()` 获取（`lock_import` 用 `poisoned.into_inner()` 恢复被 panic 毒化的锁，不让一次异常永久阻塞导入）。并发的剪贴板收藏（例如快速按 `Ctrl+Alt+S`）会排队；第二个按 SHA-256 找到第一个的暂存素材并返回 `Duplicate`（info toast，不是错误）。不要加绕过这个锁的写路径。

### `ShortcutRegistry` 状态机

`Unknown`（进程刚启动）→ `lib.rs::setup` 调用 `reconcile` 注销所有残留 OS 级快捷键，然后置 `Synced`。每次 `try_set` 以 `Synced`（成功）或 `RecoveryRequired`（回滚失败）结束。前端通过 `get_*_shortcut_status` 的 `registered` 字段间接读 `sync_state`；`RecoveryRequired` 事件以 `SetOutcome::Failed { requires_recovery: true }` 流回，应该暴露给用户（例如横幅）让他们重启或重新录制。

### `list_indexed` 锁步参数绑定与精确语法

`list_indexed(options, query, tag_ids)`（Phase 8 重写）用**锁步绑定**：SQL 的 `?` 出现顺序与 `params` Vec 完全一致（`build_view_filter` 的 group 参数 → query/精确 → tag_ids → LIMIT/OFFSET），不再手工编号。ORDER BY 由 Rust 按 `view` / `sort` 分支输出字面量（不绑定 view 参数）。视图过滤（`build_view_filter`）用 `e` 表别名 —— 每个片段必须引用 `e.xxx`，绝不 `emojis.xxx`。

查询语法：`组名*标签名` / `组名*` / `*标签名`（全角 `＊` 也支持，`:` / `：` 保留为别名）走**精确 AND**（NOCASE 精确匹配分组/标签名）；精确查询空结果时依次回退「组精确 + 标签 LIKE」→「组名子串（分组名/文件名/标签名任一）+ 标签 LIKE」→ 普通 LIKE（跨字段 OR，query 绑定 3 次）。组名子串回退让**未归组**的表情包也能 `包名*表情` 搜到（分组存在时精确优先）。**Phase 17**：回退级判定在 `resolve_search_mode`（COUNT 探测，与 offset 无关），WHERE 构建抽成 `build_search_where` 供列表（`list_indexed_impl`）与计数（`count_indexed`）共用，返回 `SearchPage { items, total }`；`sort` 扩展为 `recent` / `name-asc` / `name-desc` / `format` / `added-time` / `modified-time` 六个字面量，每个 ORDER BY 分支带 `e.id` 决胜列。

## 去哪改

- 新增 Tauri 命令？在 `src/lib/tauri.ts` 加包装、`src/types.ts` 加类型、`src-tauri/src/commands.rs` 加处理器、`src-tauri/src/lib.rs::invoke_handler` 注册。长时间工作走 `tauri::async_runtime::spawn_blocking`。对应的仓库方法应落在 `repositories/{emoji,group,tag}_repository.rs`（只有当操作跨界文件系统时才在 `services/` 里加服务 —— 参见 `trash_service`）。受管文件的 asset 协议 URL 出口是 `src/lib/tauri.ts::emojiAssetUrl`（`convertFileSrc`）；scope 配在 `tauri.conf.json` 的 `app.security.assetProtocol`（**不是** capability，且 `tauri` crate 需 `protocol-asset` feature）。
- 新增全局快捷键？在 `shortcut_registry.rs` 加一个 `ShortcutOwner` 变体；注册表处理冲突检测。前端在 `SettingsMenu.tsx` 加一行 `ShortcutterEditor`；除非用户能配置它，否则不需要新 Rust 命令。
- 新增设置？扩展 `ThemeProvider.tsx` 的 `PersistedSettings`；自动持久化到 `localStorage: emobox.settings`。设置对话框是 `src/app/SettingsMenu.tsx` 的 `SettingsDialog`（**Phase 19 起布局**：左导航是自定义 button 列——`navItem`/`navItemSelected` 与侧栏同范式，**不是 `TabList`**；`content` 整体 `colorNeutralBackground2` 灰底，右侧 `panel`（`maxWidth 640px`、唯一滚动区、切 section 经 `panelRef` 滚回顶部）内每个完整设置项经 `mergeClasses(styles.card, ...)` 成轻量白卡；**Phase 24 起长说明一律收进 `LabelInfo` 的 Info 图标 Tooltip（每行只留标题，标题自明的行连图标都不放）**，ShortcutEditor 的常驻 help 行已删除（快捷键格式约束并入快捷键行 Tooltip）；`ShortcutEditor` 成功态是紧凑状态行 `statusLine`，错误/警告才是 MessageBar（存储页隐私 MessageBar 已删除、句子并入导入与索引行 Tooltip）；关于页「当前功能」是 `featureGrid` 双列勾选清单 + **「开源依赖」chip 行**（`src/app/aboutDependencies.tsx`：simple-icons 单色品牌 SVG + Fluent 手绘简化标，点击经 `openExternalUrl` 跳 GitHub）。**Phase 27 起关于页 = 项目（仓库 chip `ldm0715/emobox` / 协议 chip GPL-3.0 带盾牌图标，均走 `handleOpenDependency`）+ 更新（`src/app/aboutUpdate.tsx`：`UpdateCard` 检查更新卡[markdown 更新说明用 Collapse + react-markdown 展开] + 自动检查更新开关 + `MirrorSourceCard` 镜像源摘要行[测速结果存卡片层 state，面板关掉摘要仍显示] + 弹出 Fluent Dialog 面板管理镜像源）**；**全部设置行带 Fluent 20px 左端 `rowIcon`（三列 `settingRow` = 图标|文案|控件，`max-width:640px` 断点下控件 `gridColumn:2` 落到文案列下方；两列 `settingRowStack` = 图标 + 纵向内容，用于素材库路径/快捷键卡）——新加设置行必须带图标，勿塞进行内的 Fluent Tooltip（性能红线见 Phase 18）；「当前功能」「开源依赖」信息清单卡例外**；关于页头副标题是应用一句话简介（原仓库行文案上移），仓库/协议行只留标签。设置弹窗标题栏是品牌行（`src/assets/logo.png` + EmoBox + 版本胶囊，版本 `getVersion()` 运行时读取、读到前不渲染胶囊，**不写死回退值**）。**坑**：Fluent `DialogContent` 自带 `overflowY: auto`，别在 `content` class 上覆盖成 `overflow: hidden`；`content` 的 grid 必须 `gridTemplateRows: minmax(0, 1fr)` 且 `DialogBody` 挂 `height: 100%`，`panel` 才有界高度、滚动才生效（详见 `docs/phase12-settings-layout.md` 与 `docs/phase19-settings-fluent-overhaul.md`）。
- 新增侧栏视图？把新值加进 `App.tsx` 的 `viewTitles`（以及 `SettingsMenu.tsx` 的默认视图下拉），扩展 `types.ts` 的 `LibraryView` 联合，并在 `App.tsx` 的 `viewPageRequest` 加分支（列表视图走 `searchEmojis`，回收站类走 `listDeletedEmojis`）。保留视图是 `all` / `favorites` / `ungrouped` / `group` / `search-recent` / `trash` —— SQL 由 `build_view_filter` 构建。
- 新增分组 / 标签 / 收藏操作？CRUD 在 `group_repository.rs` / `tag_repository.rs`；关联在 `EmojiRepository::add_to_group` / `remove_from_group` / `add_tags` / `remove_tags`；批量收藏是 `EmojiRepository::set_favorite_for_ids`（单事务，无 N+1）。分组图标名的新增只改 `src/features/library/groupIcons.ts` 注册表（Rust 侧 `set_group_icon` 对名字不做白名单校验——前端 `getGroupIcon` 回退兜底）。
- 新增网格选中 / 右键 / 批量操作？选中状态只在 `features/library/useMultiSelection.ts`（`selectedIds` 单一来源）；右键菜单是 `EmojiGrid` 里的共享 `Menu` + `EmojiItemMenu`（`multi` 区分批量）；批量处理器在 `App.tsx`，统一 `(items: IndexedImage[])` 数组签名。后端批量命令（`setEmojisFavorite` / `addEmojisToGroup` / `addTagsToEmojis` / `softDeleteToTrash` 等）已接受 `number[]`，不要再加单个 id 的新命令。
- 改卡片交互（单击/双击/悬停按钮/Tag 展示）？入口都在 `EmojiGridItem.tsx` + `useClickIntent.ts`；单双击语义变更不要绕开 `useClickIntent` 自己写 timer。大图预览是 `EmojiPreviewDialog.tsx`（状态在 App 的 `previewItem`）。给卡片加新 prop 前先确认身份稳定性（见 Phase 18 性能红线）。
- 新增回收站操作？路由到 `trash_service` —— 永远不要在该模块外改文件。更新 `trash_service::TrashResult` 的消费者（Tauri 命令）和前端回收站操作（`EmojiItemMenu` trash 模式 + 批量条；确认走共享 `ConfirmDialog`（Fluent Dialog，`src/features/library/ConfirmDialog.tsx`，App 的 `confirmState` 驱动）——UI 一致性整改起**不再用原生 `window.confirm`**，原生框不跟随应用主题）。
- 新增剪贴板*写*行为？`clipboard.rs` 是边界。别忘了 `record_image_used` 回写，让 SQLite 的 `last_used_at` / `usage_count` 保持最新。注册格式（如 `"image/gif"`）的原始字节读写只在 `platform/windows/clipboard_raw.rs`。
- 新增剪贴板*读*行为？`clipboard_collect.rs` 是边界；RGBA 必须经 `AssetService::stage_dynamic_image` 以保持 PNG 编码确定性；原始字节（GIF）路径必须经 `AssetService::stage_bytes` + `ImportService::import_bytes`（原字节保留、SHA 对原始字节算）。
- 新增搜索行为？统一路径是 `EmojiRepository::list_indexed(options, query, tag_ids)` —— 绝不要在别处写平行 SQL 字符串。锁步参数绑定（SQL `?` 与 params Vec 同步 push）+ `parse_exact_query` 精确语法是唯一做这事的地方；你要加新方法就复刻这个模式。
- 改浮层打开 / 选中文字读取？唯一入口是 `quick_search::show_quick_search`（三条路径共用；读取顺序 capture 目标 → 读选中文字 → show）。选中文字编排只在 `selection_capture.rs`，UIA 细节只在 `platform/windows/selection_reader.rs`。浮层的失焦关闭/整窗拖拽在 `QuickSearchWindow.tsx` / `QuickSearchPanel.tsx` + `overlayDragGuard.ts`；DWM 圆角修复只在 `platform/windows/dwm.rs`（`lib.rs::setup` 调用）。
- 实现历史和各阶段设计笔记：`docs/implementation-plan.md`、`docs/windows-image-clipboard.md`、`docs/system-tray-recent-usage.md`、`docs/search-overlay-global-shortcut.md`、`docs/phase5-clipboard-probe-results.md`、`docs/phase6-groups-tags-trash.md`、`docs/phase7-chat-paste.md`、`docs/phase8-import-optimization.md`、`docs/phase9-multiselect-context-menu.md`、`docs/phase10-search-syntax-filename-tags.md`、`docs/phase11-ui-polish-time-sort.md`（主题按钮图标、移除 logo、按添加/修改时间排序、迁移版本复用修复）、`docs/phase12-settings-layout.md`（设置界面布局重构、Fluent 滚动容器坑）、`docs/phase13-sidebar-groups-redesign.md`（侧边栏分组区重设计：搜索按钮收纳、置顶、`flex:1` 布局、空间压缩）、`docs/phase14-sidebar-divider-flex-fix.md`（Fluent Divider 默认 `flex-grow:1` 挤占分组区空间的修复）、`docs/phase15-selection-search.md`（选中文字自动搜索：UIA + Ctrl+C 兜底、focus_restore E0133 修复、快捷键路径漏捕获修复）、`docs/phase16-gif-animation.md`（GIF 动画全链路：assetProtocol 悬停播放、Win32 clipboard_raw 读写 `"image/gif"`、stage_bytes/import_bytes、追加式 SetClipboardData、开发机剪贴板被占的 probe 记录）、`docs/phase17-pagination-selectall.md`（主窗口分页无限滚动：`{items, total}` 分页契约、排序下推 SQL + id 决胜列、搜索回退链 COUNT 探测修复、计数与已加载解耦、EmojiGrid 哨兵双通道、多选「全选=已加载项」按钮）、`docs/phase18-emoji-card-interaction.md`（卡片交互重设计：单击复制/双击预览/useClickIntent 消歧、文件名与 Tag 下移、EmojiPreviewDialog、Tooltip×800 卡死回归与 memo 化修复、DialogSurface fit-content 坑）、`docs/phase19-settings-fluent-overhaul.md`（设置弹窗 Fluent 化：自定义导航 + Win11 灰底白卡、长说明收 Info Tooltip、拖入提示只盖图片区、color-scheme 滚动条暗色适配、侧栏底部按钮统一）、`docs/ui-consistency-audit.md`（UI 一致性审计与整改：`::selection` 品牌色、窗口 theme 跟随系统、ConfirmDialog 替换原生 confirm/prompt、navItemStyles/pickerDialogStyles/cardStyles 样式去重）、`docs/ui-consistency-remediation.md`（前述整改的实施记录：逐项变更明细、makeStyles 展开外部对象的 TS 类型坑、palette 红无 Hover/Pressed 变体、遗留事项；以及 2026-09-01 四轮后续修复——回收站操作收紧、通知模型统一、收藏视图即时刷新 + 切视图 keep-previous 闪屏修复 + 浮层透明圆角窗口与 Win10 setShadow 坑、主题菜单选中行行高修复）、`docs/phase20-quick-search-refactor.md`（浮层重构：置顶分组行/状态行/20 条分页+结果缓存/失焦关闭+拖拽守卫/整窗拖拽/Win10 圆角 DWM 属性+区域裁剪修复与「勿在可见期调 set_shadow」教训）、`docs/phase21-clipboard-dib-fallback.md`（剪贴板收藏原生回退链：read_image 失败后 PNG 注册格式 → CF_DIBV5 → CF_DIB、Chrome 形态 32bpp BI_RGB+alphaMask DIB 手解 BGRA 保 alpha、勿改回 arboard tweak 方案）、`docs/phase22-import-into-current-group.md`（导入自动加入当前分组：`target_group_id` 贯穿三个导入命令与 clipboard 四通道、文件夹导入抑制子分组自动建组、发起时捕获目标分组 + 停留分组视图经 viewReloadTick 重拉）、`docs/phase23-import-loading-indicator.md`（导入加载条四入口拉齐：剪贴板收藏接入、isImporting 计数托管 + 600ms 最短可见、拖放其实一直有加载条只是太快感知不到）、`docs/phase24-settings-declutter-about-deps.md`（设置页文案全收 LabelInfo Tooltip + ShortcutEditor 删常驻 help、关于页删开发计划改开源依赖 chips（simple-icons 单色标 + Fluent 手绘标）、`open_external_url` 命令与 host 白名单、getVersion 运行时读取）、`docs/phase25-close-behavior-dialog.md`（主窗口关闭三态：CloseBehaviorState 内存镜像 + on_window_event 三分支、CloseActionDialog 询问窗勾「记住」写回 `closeToTray` 与设置开关联动、capability 放行 `core:window:allow-hide`）、`docs/phase26-tray-menu-window.md`（托盘菜单自绘窗口：原生 Win32 菜单不可样式化所以整体换 Fluent 窗口、左键开主窗口右键弹菜单、事件 rect 定位 + 显示器 clamp、先藏后动作防粘贴目标抓到菜单自己、settings-open-requested 联动设置弹窗、透明窗口接入清单）、`docs/phase27-updates-and-branding.md`（自动更新 + 镜像源 + 品牌改造：为何自定义 updater 不用官方插件、镜像前缀协议与测速排序、SHA-256 取代 minisign 的取舍、CHANGES.md→latest.json→应用内 markdown 渲染链路、GPL-3.0-only、logo/品牌改名清单与 r### 原始字符串/Button 无 loading prop 等坑）。README 有产品级概览；AGENTS.md 是本文件的同步副本（更新本文件后整体覆盖它）。

> **注意**：本文件（`CLAUDE.md`）与 `AGENTS.md` 内容保持一致（更新时同步覆盖），README.md 也在 eb05ad2 同步过产品级描述，三者均不再有「Phase 5 前旧描述」问题。历史细节与决策记录以 `docs/` 各 phase 文档为准；commit 遵循 Conventional Commits。
